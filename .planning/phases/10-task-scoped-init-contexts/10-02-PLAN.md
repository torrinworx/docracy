---
phase: 10-task-scoped-init-contexts
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/cli/src/main.rs
  - README.md
autonomous: true
requirements: ["TBD"]

must_haves:
  truths:
    - "CLI `docracy init` output now includes `task_scope` and `task_context_documents` without removing existing fields"
    - "Setting DOCRACY_TASK_SCOPE influences only the computed task_context_documents subset"
  artifacts:
    - path: "crates/cli/src/main.rs"
      provides: "CLI Init passes optional task scope into core init_bundle_scoped"
      contains: "DOCRACY_TASK_SCOPE"
    - path: "README.md"
      provides: "User-facing contract documents task-scoped init fields"
      contains: "task_context_documents"
  key_links:
    - from: "crates/cli/src/main.rs"
      to: "docracy_core::init_bundle_scoped"
      via: "Init command path"
      pattern: "init_bundle_scoped"
---

<objective>
Wire task-scope selection into the CLI `Init` path via an environment variable, and document the extended Init output shape.

Purpose: Let task runners request a specialty init context subset without adding a new CLI subcommand or changing the base contract.
Output: `docracy init` JSON includes `task_scope` + `task_context_documents` computed by core.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@governance/CONSTITUTION.md
@crates/cli/src/main.rs

Note: Init must still return all active `type=context` docs as `context_documents`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DOCRACY_TASK_SCOPE env wiring to CLI init</name>
  <files>crates/cli/src/main.rs</files>
  <read_first>
    <file>crates/cli/src/main.rs</file>
    <file>crates/core/src/lib.rs</file>
    <file>crates/core/src/service.rs</file>
    <file>governance/CONSTITUTION.md</file>
  </read_first>
  <action>
    In the `Command::Init {}` match arm in `crates/cli/src/main.rs`:
    1) Read an optional task scope from the environment variable `DOCRACY_TASK_SCOPE`.
       - Use: `let task_scope = std::env::var("DOCRACY_TASK_SCOPE").ok();`
       - If present, trim whitespace; if empty after trim, treat as None.

    2) Call the new core helper (added in Plan 10-01):
       - Replace `init_bundle(&mut repo, &governance, &clock, &ids)` with
         `init_bundle_scoped(&mut repo, &governance, &clock, &ids, task_scope.as_deref())`.

    3) Keep the existing JSON fields exactly as-is:
       - `governance: { files: [...] }`
       - `context_documents: [...]`
       And extend the output JSON to include:
       - `task_scope: out.task_scope`
       - `task_context_documents: out.task_context_documents`
       Do not rename `context_documents`.

    4) Add a small unit test module in `crates/cli/src/main.rs` that tests the trim/empty behavior for `DOCRACY_TASK_SCOPE` parsing via a small helper function you introduce (e.g., `fn normalize_task_scope(raw: Option<String>) -> Option<String>`).
       - Test that `Some("  planning  ".to_string()) -> Some("planning".to_string())`.
       - Test that `Some("   ".to_string()) -> None`.
       - Test that `None -> None`.
  </action>
  <acceptance_criteria>
    <criterion>`crates/cli/src/main.rs` references the exact string `DOCRACY_TASK_SCOPE`.</criterion>
    <criterion>The CLI init code calls `init_bundle_scoped(..., task_scope.as_deref())`.</criterion>
    <criterion>CLI init JSON output includes top-level keys `task_scope` and `task_context_documents`.</criterion>
    <criterion>A unit test exists in the CLI crate covering whitespace trimming and empty-to-None normalization for task scope.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-cli --quiet</automated>
  </verify>
  <done>
    CLI init can emit task-scoped context subset metadata without breaking existing init consumers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document task-scoped init fields in README</name>
  <files>README.md</files>
  <read_first>
    <file>README.md</file>
    <file>governance/CONSTITUTION.md</file>
  </read_first>
  <action>
    Update `README.md` where Init is described to include:
    - A note that init continues to return ALL active context documents in `context_documents`.
    - An explanation of optional task scoping:
      - Environment variable: `DOCRACY_TASK_SCOPE` (string; whitespace trimmed; empty treated as unset)
      - Output fields:
        - `task_scope`: null or the normalized scope string
        - `task_context_documents`: [] when `task_scope` is null; otherwise includes:
          * all unscoped contexts (no `extensions.task_scopes`)
          * plus contexts whose `extensions.task_scopes` includes the scope string
    - Document the extension key used by this feature: `extensions.task_scopes` (array of strings).

    Keep the README wording explicit that this is an additive convenience field; it does not change the meaning of “active context docs”.
  </action>
  <acceptance_criteria>
    <criterion>`README.md` contains the literal string `DOCRACY_TASK_SCOPE`.</criterion>
    <criterion>`README.md` contains the literal string `task_context_documents`.</criterion>
    <criterion>`README.md` contains the literal string `extensions.task_scopes`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n "DOCRACY_TASK_SCOPE|task_context_documents|extensions\.task_scopes" README.md</automated>
  </verify>
  <done>
    The public contract documents the new Init output fields and how to author task-scoped context docs.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-cli` passes.
- README reflects the extended Init output without implying filtered active contexts.
</verification>

<success_criteria>
- CLI supports task-scoped init context selection via env var and emits the new JSON fields.
</success_criteria>

<output>
After completion, create `.planning/phases/10-task-scoped-init-contexts/10-02-SUMMARY.md`
</output>
