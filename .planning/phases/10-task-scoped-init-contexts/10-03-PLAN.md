---
phase: 10-task-scoped-init-contexts
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - crates/mcp/src/config.rs
  - crates/mcp/src/runtime.rs
  - crates/mcp/src/operations.rs
  - crates/mcp/src/tools.rs
  - crates/mcp/src/bin/docracy-mcp.rs
  - crates/mcp/README.md
autonomous: true
requirements: ["TBD"]

must_haves:
  truths:
    - "MCP Init tool output includes task_scope + task_context_documents while preserving existing governance/context_documents fields"
    - "MCP server can be configured (via env) to compute a task-scoped context subset on Init"
  artifacts:
    - path: "crates/mcp/src/runtime.rs"
      provides: "Runtime carries optional task scope"
      contains: "task_scope"
    - path: "crates/mcp/src/tools.rs"
      provides: "Init tool JSON includes task_scope + task_context_documents"
      contains: "task_context_documents"
    - path: "crates/mcp/README.md"
      provides: "MCP contract documents new init fields"
      contains: "task_context_documents"
  key_links:
    - from: "crates/mcp/src/tools.rs"
      to: "crate::operations::init_bundle_runtime"
      via: "init tool handler"
      pattern: "init_bundle_runtime"
---

<objective>
Expose task-scoped init context selection through the MCP server runtime (env-configured), and return the computed fields from the MCP Init tool.

Purpose: Allow MCP clients to receive both the full active contexts and a ready-to-use task-scoped subset from Init.
Output: MCP Init tool JSON includes `task_scope` and `task_context_documents`.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@governance/CONSTITUTION.md
@crates/mcp/src/tools.rs
@crates/mcp/src/runtime.rs
@crates/mcp/src/bin/docracy-mcp.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DOCRACY_TASK_SCOPE parsing + runtime propagation for MCP</name>
  <files>crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/bin/docracy-mcp.rs, crates/mcp/src/operations.rs</files>
  <read_first>
    <file>crates/mcp/src/config.rs</file>
    <file>crates/mcp/src/runtime.rs</file>
    <file>crates/mcp/src/bin/docracy-mcp.rs</file>
    <file>crates/mcp/src/operations.rs</file>
    <file>crates/core/src/lib.rs</file>
  </read_first>
  <action>
    1) In `crates/mcp/src/config.rs`, add a helper function:
       - `pub fn parse_task_scope(value: Option<&str>) -> Option<String>`
       - Behavior: if None -> None; if Some(s) -> trim; if empty after trim -> None; else Some(trimmed.to_string()).
       - Add unit tests similar to `parse_workspace_id` tests.

    2) Extend `McpStartupConfig` (in `crates/mcp/src/config.rs`) to carry `task_scope: Option<String>`.
       - Update `McpStartupConfig::new(...)` signature to accept this new field.
       - Update any construction call sites accordingly.

    3) Extend `McpRuntime` (in `crates/mcp/src/runtime.rs`) to carry `pub task_scope: Option<String>`.
       - In `bootstrap`, copy it from `McpStartupConfig`.

    4) In `crates/mcp/src/bin/docracy-mcp.rs`, read env var `DOCRACY_TASK_SCOPE` and parse via `parse_task_scope(...)`.
       - Keep existing stdio safety behavior (logs to stderr).
       - Pass `task_scope` into `McpStartupConfig::new(...)`.

    5) In `crates/mcp/src/operations.rs`, add a scoped init wrapper:
       - Update `init_bundle_runtime(runtime)` to call `docracy_core::init_bundle_scoped(..., runtime.task_scope.as_deref())` (via a crate::operations helper if you prefer), so InitBundleResult includes the task fields.
  </action>
  <acceptance_criteria>
    <criterion>`crates/mcp/src/bin/docracy-mcp.rs` references the exact string `DOCRACY_TASK_SCOPE`.</criterion>
    <criterion>`McpStartupConfig` and `McpRuntime` each have a `task_scope: Option<String>` field.</criterion>
    <criterion>`crates/mcp/src/config.rs` defines `parse_task_scope` and includes unit tests covering trim/empty.</criterion>
    <criterion>`init_bundle_runtime` passes `runtime.task_scope.as_deref()` into `init_bundle_scoped` (directly or indirectly).</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-mcp --quiet</automated>
  </verify>
  <done>
    MCP runtime can carry an optional task scope and uses it to compute the task-scoped context subset during Init.
  </done>
</task>

<task type="auto">
  <name>Task 2: Return + document task-scoped init fields in MCP tool output</name>
  <files>crates/mcp/src/tools.rs, crates/mcp/README.md</files>
  <read_first>
    <file>crates/mcp/src/tools.rs</file>
    <file>crates/mcp/README.md</file>
    <file>governance/CONSTITUTION.md</file>
  </read_first>
  <action>
    1) In `crates/mcp/src/tools.rs` Init tool handler, extend the JSON response to include:
       - `"task_scope": out.task_scope`
       - `"task_context_documents": out.task_context_documents`
       Preserve existing fields:
       - `governance.files`
       - `context_documents`

    2) Update `crates/mcp/README.md` to reflect the extended Init output by adding the two new keys in its JSON example.
       Also document:
       - Env var: `DOCRACY_TASK_SCOPE`
       - Extension key used for scoping: `extensions.task_scopes` (array of strings)
       - Reminder: `context_documents` remains the full active set; the task-scoped field is additive.
  </action>
  <acceptance_criteria>
    <criterion>`crates/mcp/src/tools.rs` includes JSON keys `task_scope` and `task_context_documents` in the init response.</criterion>
    <criterion>`crates/mcp/README.md` contains `DOCRACY_TASK_SCOPE` and `task_context_documents`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>rg -n "task_context_documents|DOCRACY_TASK_SCOPE" crates/mcp/src/tools.rs crates/mcp/README.md</automated>
  </verify>
  <done>
    MCP Init tool returns and documents task-scoped init context fields.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-mcp` passes.
- Init output still includes `context_documents` with all active contexts.
</verification>

<success_criteria>
- MCP server supports task-scoped init convenience fields configured via env var.
</success_criteria>

<output>
After completion, create `.planning/phases/10-task-scoped-init-contexts/10-03-SUMMARY.md`
</output>
