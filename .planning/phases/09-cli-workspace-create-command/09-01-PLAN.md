---
phase: 09-cli-workspace-create-command
plan: 01
type: execute
wave: 1
depends_on:
  - 08-01
  - 08-02
files_modified:
  - crates/cli/src/main.rs
  - crates/cli/Cargo.toml
  - crates/postgres/src/lib.rs
  - crates/postgres/tests/postgres_integration.rs
  - crates/cli/tests/cli_stderr.rs
  - crates/cli/tests/workspace_create.rs
  - crates/cli/tests/fixtures/create_workspace_invalid_id.stderr.json
  - README.md
autonomous: true
requirements:
  - WS-05
  - WS-06
  - WS-07
must_haves:
  truths:
    - "The CLI can create a workspace row and return its UUID without involving MCP."
    - "Workspace IDs are generated with a UUID library by default, and reserved nil UUIDs stay tied to the shared global fallback only."
    - "The workspace-create flow keeps the existing structured stdout/stderr contract and surfaces invalid IDs as CLI validation errors."
  artifacts:
    - path: "crates/cli/src/main.rs"
      provides: "Nested workspace CLI command and JSON response wiring"
    - path: "crates/cli/Cargo.toml"
      provides: "UUID dependency for CLI workspace generation/parsing"
    - path: "crates/postgres/src/lib.rs"
      provides: "Workspace insert helper on the Postgres adapter"
    - path: "crates/postgres/tests/postgres_integration.rs"
      provides: "Workspace row insertion coverage"
    - path: "crates/cli/tests/workspace_create.rs"
      provides: "Workspace-create CLI regression coverage"
    - path: "crates/cli/tests/cli_stderr.rs"
      provides: "Structured stderr validation for bad workspace IDs"
    - path: "README.md"
      provides: "Manual operator flow for generating and exporting workspace IDs"
  key_links:
    - from: "docracy workspace create"
      to: "INSERT INTO workspaces (id)"
      via: "Postgres-only workspace provisioning helper"
    - from: "workspace_id argument"
      to: "Uuid::new_v4() default generation"
      via: "CLI generates an ID when one is omitted"
    - from: "invalid workspace id"
      to: "structured stderr JSON"
      via: "existing CLI error envelope"
    - from: "workspace provisioning docs"
      to: "WORKSPACE_ID"
      via: "manual bootstrap path for later MCP binding"
---

<objective>
Add an interim CLI-only `workspace create` command so operators can provision a workspace UUID and database row manually, then feed that UUID into `WORKSPACE_ID` for MCP sessions.

Purpose: make workspace bootstrap explicit without expanding the MCP tool surface.
Output: a workspace CLI command, Postgres insert helper, tests, and operator-facing docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@crates/cli/src/main.rs
@crates/cli/Cargo.toml
@crates/postgres/src/lib.rs
@crates/postgres/tests/postgres_integration.rs
@crates/cli/tests/cli_stderr.rs
@README.md
@migrations/0006_workspace_tenancy.sql

<interfaces>
From `crates/cli/src/main.rs` (current surface):
```rust
enum Command {
    Init {},
    Create { input: Option<String> },
    Query { input: Option<String> },
    Read { input: Option<String> },
    Update { input: Option<String> },
    Migrate,
}
```

Planned CLI surface for this phase:
```rust
enum Command {
    Workspace {
        command: WorkspaceCommand,
    },
}

enum WorkspaceCommand {
    Create { workspace_id: Option<String> },
}
```

Planned Postgres helper:
```rust
pub async fn create_workspace(&self, id: uuid::Uuid) -> Result<(), RepoError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the CLI workspace-create command and Postgres insert helper</name>
  <files>crates/cli/src/main.rs, crates/cli/Cargo.toml, crates/postgres/src/lib.rs</files>
  <read_first>crates/cli/src/main.rs, crates/cli/Cargo.toml, crates/postgres/src/lib.rs, migrations/0006_workspace_tenancy.sql</read_first>
  <behavior>
    - Add a nested `workspace create` command to the CLI.
    - Generate a UUID when no explicit workspace ID is supplied.
    - Accept an optional `--workspace-id` override for scripted provisioning.
    - Reject the reserved nil UUID and keep invalid input on the existing structured error path.
    - Insert the workspace row through the Postgres adapter rather than embedding raw SQL in the CLI.
  </behavior>
  <action>Extend the CLI command parser with `workspace create`, add the `uuid` dependency to the CLI crate, and thread a small adapter helper through `PgRepository` to insert into `workspaces`. Keep the command output JSON and keep the MCP crate untouched. The command should return the created workspace UUID on stdout so an operator can export it into `WORKSPACE_ID`.</action>
  <acceptance_criteria>
    - `docracy workspace create` is available from the CLI.
    - The command generates a UUID by default and accepts an explicit UUID when provided.
    - Nil UUIDs are rejected because the reserved global workspace remains special.
    - Workspace rows are inserted through the Postgres adapter.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-cli --tests workspace_create</automated>
  </verify>
  <done>The CLI can provision a workspace row and return the UUID without exposing any MCP workspace tool.</done>
</task>

<task type="auto">
  <name>Task 2: Add CLI regression tests and operator docs</name>
  <files>crates/cli/tests/workspace_create.rs, crates/cli/tests/cli_stderr.rs, crates/cli/tests/fixtures/create_workspace_invalid_id.stderr.json, README.md, crates/postgres/tests/postgres_integration.rs</files>
  <read_first>crates/cli/tests/cli_stderr.rs, crates/cli/tests/workspace_create.rs, README.md, crates/postgres/tests/postgres_integration.rs</read_first>
  <action>Add CLI coverage for successful workspace creation, explicit UUID handling, and invalid UUID stderr output. Add Postgres integration coverage proving the workspace row is written as expected. Update the top-level README with the manual operator flow: create a workspace with the CLI, export the returned UUID, and pass it through `WORKSPACE_ID` for MCP startup. Keep the docs explicit that workspace management is CLI-only and not part of the MCP tool surface.</action>
  <acceptance_criteria>
    - CLI tests cover generated IDs and invalid input.
    - Postgres tests cover workspace row creation.
    - README documents the manual CLI-to-MCP bootstrap path.
    - No MCP tool files are modified for workspace creation.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-cli --test workspace_create</automated>
    <automated>cargo test -p docracy-postgres --test postgres_integration workspace</automated>
  </verify>
  <done>The interim workspace provisioning flow is documented and regression-covered.</done>
</task>

</tasks>

<verification>
- `docracy workspace create` returns a JSON workspace UUID on stdout.
- The CLI rejects malformed and nil workspace IDs with the existing structured stderr envelope.
- Postgres integration confirms a workspace row is inserted.
- The MCP crate remains unchanged and exposes no workspace management tool.
</verification>

<success_criteria>
- Operators can provision a workspace manually before binding it to MCP.
- Workspace creation stays CLI-only and does not expand the MCP tool contract.
- The returned workspace UUID can be copied directly into `WORKSPACE_ID`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-cli-workspace-create-command/09-01-SUMMARY.md`
</output>
