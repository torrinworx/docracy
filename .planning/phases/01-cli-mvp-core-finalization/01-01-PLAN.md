---
phase: 01-cli-mvp-core-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/service.rs
  - crates/core/src/repository.rs
  - crates/core/src/memory.rs
  - crates/postgres/src/lib.rs
  - crates/core/src/errors.rs
autonomous: true
requirements:
  - DOC-01
  - DOC-03
  - REV-01
  - REV-02
  - REV-03
  - REV-04
  - PG-01
  - PG-02
must_haves:
  truths:
    - Stale updates are rejected when the expected head revision does not match.
    - Successful updates append one immutable revision and advance the document head atomically.
    - Core and Postgres repository paths agree on the same OCC behavior.
  artifacts:
    - crates/core/src/service.rs
    - crates/core/src/repository.rs
    - crates/core/src/memory.rs
    - crates/postgres/src/lib.rs
  key_links:
    - update_document uses an expected head revision and the repository enforces it inside the write transaction.
    - create/read/update behavior stays aligned between MemoryRepository and PgRepository.
---

<objective>
Finalize revision-safe document updates.

Purpose: make concurrent writes deterministic and keep the immutable revision chain correct.
Output: core update flow, repository trait, and Postgres adapter all enforce expected-head OCC.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@crates/core/src/service.rs
@crates/core/src/repository.rs
@crates/core/src/memory.rs
@crates/postgres/src/lib.rs
@crates/core/src/errors.rs
@crates/core/src/revision.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Require an expected head revision for updates</name>
  <files>crates/core/src/service.rs, crates/core/src/repository.rs, crates/core/src/errors.rs</files>
  <read_first>crates/core/src/service.rs, crates/core/src/repository.rs, crates/core/src/errors.rs, crates/core/src/revision.rs</read_first>
  <action>Add a required expected-head field to `UpdateDocumentInput`, compare it against the document's current head before composing a new revision, and surface a clear conflict when the revision is stale. Keep the immutable revision chain behavior unchanged for successful updates.</action>
  <verify>cargo test -p docracy-core service::tests::create_then_update_creates_revision_chain</verify>
  <acceptance_criteria>
    - `UpdateDocumentInput` includes a required expected-head field.
    - Stale updates return a conflict instead of silently overwriting the current head.
    - Successful updates still create version 2 from version 1 in the service test.
  </acceptance_criteria>
  <done>Core update flow rejects stale writes and still appends revisions atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce OCC in the adapters</name>
  <files>crates/core/src/memory.rs, crates/postgres/src/lib.rs</files>
  <read_first>crates/core/src/memory.rs, crates/postgres/src/lib.rs, migrations/0001_documents_and_revisions.sql</read_first>
  <action>Update both repository adapters so `update_document_with_revisions` only succeeds when the current document head matches the expected revision. Make the Postgres write transactional and fail cleanly on a stale head; keep the memory adapter behavior aligned for tests and local usage.</action>
  <verify>cargo test -p docracy-core</verify>
  <acceptance_criteria>
    - MemoryRepository and PgRepository enforce the same stale-write rule.
    - Postgres updates remain atomic across document and revision writes.
    - Existing create/read/query behavior still compiles and tests pass.
  </acceptance_criteria>
  <done>Repository adapters enforce the same revision OCC rule.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p docracy-core` passes
- [ ] The update path requires an expected head revision
- [ ] Memory and Postgres repositories reject stale writes consistently
</verification>

<success_criteria>

- All tasks completed
- Revision updates are OCC-safe
- No new regressions in core document or revision behavior

</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-mvp-core-finalization/01-01-SUMMARY.md`
</output>
