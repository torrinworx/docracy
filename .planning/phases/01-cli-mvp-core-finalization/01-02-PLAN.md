---
phase: 01-cli-mvp-core-finalization
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/core/src/service.rs
  - crates/core/src/governance.rs
  - crates/core/src/document.rs
  - crates/core/src/validation.rs
autonomous: true
requirements:
  - DOC-02
  - DOC-04
  - DOC-05
  - GOV-01
  - GOV-02
  - GOV-03
  - GOV-04
must_haves:
  truths:
    - `init` is rerunnable and returns the active context documents.
    - The repo-owned constitution can be reconciled by init but not modified by normal user-facing create/update flows.
    - Constitution mismatches are surfaced clearly rather than silently ignored.
  artifacts:
    - crates/core/src/service.rs
    - crates/core/src/governance.rs
    - crates/core/src/document.rs
  key_links:
    - init_bundle can bootstrap or reconcile constitution state without exposing constitution mutation to regular document writes.
    - Public validation still rejects constitution creation through normal document input.
---

<objective>
Finalize governance seeding and constitution immutability.

Purpose: keep the database aligned with the repo-owned constitution while preserving rerunnable init behavior.
Output: init returns governance + active context docs, and normal interfaces reject constitution mutation.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@crates/core/src/service.rs
@crates/core/src/governance.rs
@crates/core/src/document.rs
@crates/core/src/validation.rs
@governance/CONSTITUTION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make init rerunnable and constitution-safe</name>
  <files>crates/core/src/service.rs, crates/core/src/governance.rs</files>
  <read_first>crates/core/src/service.rs, crates/core/src/governance.rs, crates/core/src/document.rs, crates/core/src/validation.rs, governance/CONSTITUTION.md</read_first>
  <action>Keep `init_bundle` rerunnable, but route constitution repair through an internal system-only helper so normal document mutation rules remain strict. Return the active context documents after init and preserve the repo-owned constitution text as the source of truth.</action>
  <verify>cargo test -p docracy-core init_bootstraps_missing_constitution</verify>
  <acceptance_criteria>
    - `init_bundle` still returns governance files and active context documents.
    - Constitution reconciliation is internal to init and does not rely on public mutation paths.
    - The constitution file remains the source of truth.
  </acceptance_criteria>
  <done>Init works as a rerunnable bootstrap for governance and context.</done>
</task>

<task type="auto">
  <name>Task 2: Reject constitution changes through user-facing document input</name>
  <files>crates/core/src/document.rs, crates/core/src/validation.rs</files>
  <read_first>crates/core/src/document.rs, crates/core/src/validation.rs</read_first>
  <action>Keep the reserved constitution type blocked on regular document creation and any public update path that would attempt to mutate it. Preserve the existing validation error shape for normal callers.</action>
  <verify>cargo test -p docracy-core document::tests::new_document_rejects_constitution_type</verify>
  <acceptance_criteria>
    - Normal `NewDocument` validation still rejects constitution documents.
    - The validation error remains explicit and stable.
  </acceptance_criteria>
  <done>Normal document input cannot create or mutate constitution content.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p docracy-core` passes
- [ ] Init still returns active context documents
- [ ] Regular document input still rejects constitution creation
</verification>

<success_criteria>

- All tasks completed
- Constitution immutability is enforced through supported interfaces
- Init remains rerunnable and deterministic

</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-mvp-core-finalization/01-02-SUMMARY.md`
</output>
