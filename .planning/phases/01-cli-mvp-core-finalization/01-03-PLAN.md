---
phase: 01-cli-mvp-core-finalization
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/cli/src/main.rs
  - README.md
autonomous: true
requirements:
  - QRY-01
  - QRY-02
  - QRY-03
  - QRY-04
  - CLI-01
  - CLI-02
  - CLI-03
  - PG-03
must_haves:
  truths:
    - The CLI exposes the final JSON contract for init, create, read, query, update, and migrate.
    - Errors are machine-readable and exit non-zero.
    - Update payloads carry the expected head revision so the CLI can participate in OCC.
  artifacts:
    - crates/cli/src/main.rs
    - README.md
  key_links:
    - CLI JSON input/output shape matches the final core service contract.
    - Command examples in the README reflect the actual supported payloads.
---

<objective>
Finalize the CLI contract and keep query behavior explicit.

Purpose: make Docracy automation-friendly from the shell while preserving the stable query semantics already in the core.
Output: CLI update/read/query behavior is machine-readable and documented.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@crates/cli/src/main.rs
@crates/core/src/query.rs
@crates/core/src/errors.rs
@crates/core/src/service.rs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the final JSON CLI update contract</name>
  <files>crates/cli/src/main.rs</files>
  <read_first>crates/cli/src/main.rs, crates/core/src/service.rs, crates/core/src/errors.rs</read_first>
  <action>Add the expected head revision field to the CLI update input, keep the command outputs as stable JSON objects, and emit structured machine-readable errors with a non-zero exit code. Keep migrations enabled by default and keep the `--database-url` override behavior intact.</action>
  <verify>cargo check -p docracy-cli</verify>
  <acceptance_criteria>
    - `update` JSON input includes the expected head revision field.
    - CLI failures print structured JSON and exit 1.
    - `DATABASE_URL` and `--database-url` precedence remains unchanged.
  </acceptance_criteria>
  <done>CLI can drive the finalized core contract from JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Align docs with the final CLI surface</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>Update the examples and prose so they show the actual CLI payloads and error behavior, including the expected revision field for updates. Keep the docs aligned with the deferred v1 extension-search rule.</action>
  <verify>grep -n "expected_revision" README.md</verify>
  <acceptance_criteria>
    - README examples match the real update payload.
    - The docs do not imply v1 extension search support.
  </acceptance_criteria>
  <done>CLI docs match the code that ships in phase 1.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p docracy-cli` passes
- [ ] README shows the expected revision field for updates
- [ ] CLI error output is structured JSON
</verification>

<success_criteria>

- All tasks completed
- CLI contract is stable and machine-readable
- Query/search semantics remain explicit in docs and output

</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-mvp-core-finalization/01-03-SUMMARY.md`
</output>
