---
phase: 03-stabilization-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_repository_invariants.sql
  - crates/postgres/tests/postgres_integration.rs
  - crates/cli/src/main.rs
  - README.md
autonomous: true
requirements: [TST-01, TST-02, TST-03]
must_haves:
  truths:
    - "Database rejects cross-document revision corruption"
    - "The migrate subcommand behaves consistently even with --no-migrate"
    - "README matches the shipped read/query/update contract"
  artifacts:
    - path: "migrations/0004_repository_invariants.sql"
      provides: "DB-side revision consistency triggers and pagination-aligned indexes"
      contains: "document_revisions_parent_same_document"
    - path: "crates/postgres/tests/postgres_integration.rs"
      provides: "Regression coverage for malformed revision graphs and query stability"
      contains: "cross-document revision corruption"
    - path: "crates/cli/src/main.rs"
      provides: "Migrate command startup behavior and regression tests"
      contains: "Command::Migrate"
    - path: "README.md"
      provides: "User-facing contract examples that match implementation"
      contains: "expected_revision"
  key_links:
    - from: "migrations/0004_repository_invariants.sql"
      to: "crates/postgres/tests/postgres_integration.rs"
      via: "malformed-revision regression tests"
      pattern: "parent_revision_id.*document_id"
    - from: "crates/cli/src/main.rs"
      to: "Command::Migrate"
      via: "startup migration gate"
      pattern: "no_migrate"
    - from: "README.md"
      to: "crates/core/src/query.rs"
      via: "documented query/read contract"
      pattern: "extensions.*deferred"
---

<objective>
Stabilize the v1 core by closing the remaining validation gaps: harden repository invariants, fix the CLI migrate edge case, and remove documentation drift.

Purpose: Keep the Postgres-backed document store trustworthy under corruption scenarios and make the shipped contract match what agents actually get.
Output: One new migration, expanded regression coverage, a CLI behavior fix, and aligned README examples.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@crates/postgres/tests/postgres_integration.rs
@crates/cli/src/main.rs
@migrations/0001_documents_and_revisions.sql
@README.md
</context>

<interfaces>
From `crates/cli/src/main.rs`:
```rust
#[derive(Subcommand, Debug)]
enum Command {
    Init { governance_dir: PathBuf },
    Create { input: Option<String> },
    Query { input: Option<String> },
    Read { input: Option<String> },
    Update { input: Option<String> },
    Migrate,
}

#[derive(Debug, Deserialize)]
struct UpdateInput {
    id: DocumentId,
    #[serde(alias = "expected_head")]
    expected_revision: RevisionId,
    content: Option<Value>,
    extensions: Option<Map<String, Value>>,
    status: Option<String>,
}
```

From `crates/postgres/tests/postgres_integration.rs`:
```rust
async fn isolated_repo(url: &str) -> (PgRepository, SchemaGuard)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden document/revision integrity in Postgres</name>
  <files>migrations/0004_repository_invariants.sql, crates/postgres/tests/postgres_integration.rs</files>
  <read_first>migrations/0001_documents_and_revisions.sql, crates/postgres/tests/postgres_integration.rs, .planning/codebase/CONCERNS.md, .planning/codebase/TESTING.md</read_first>
  <behavior>
    - Inserting or updating a revision whose `parent_revision_id` points at a revision from another `document_id` fails with a constraint/trigger error.
    - Setting `documents.current_revision_id` to a revision from a different `document_id` fails with a constraint/trigger error.
    - Existing valid init/create/update/query flows still pass after the migration and the pagination order remains stable.
  </behavior>
  <acceptance_criteria>
    - `migrations/0004_repository_invariants.sql` defines a trigger/function that enforces same-document parent revision lineage.
    - `migrations/0004_repository_invariants.sql` defines a trigger/function that enforces `documents.current_revision_id` points to a revision owned by the same document.
    - `migrations/0004_repository_invariants.sql` adds composite indexes aligned to cursor pagination on `documents(created_at, id)` and `documents(modified_at, id)`.
    - `crates/postgres/tests/postgres_integration.rs` includes regression coverage for both malformed revision-graph cases.
  </acceptance_criteria>
  <action>Create a new migration that adds DB-side consistency checks for revision lineage and current revision ownership, plus the composite indexes that match the `(timestamp, id)` keyset cursor pattern. Extend the Postgres integration harness with raw-SQL regression tests that prove the database rejects cross-document corruption while the existing init/create/update/query coverage still passes through `PgRepository`.</action>
  <verify><automated>cargo test -p docracy-postgres --test postgres_integration</automated></verify>
  <done>The database rejects cross-document revision corruption, and the integration harness proves the stored contract still works end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix the CLI migrate edge case</name>
  <files>crates/cli/src/main.rs</files>
  <read_first>crates/cli/src/main.rs, .planning/codebase/CONCERNS.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <behavior>
    - `Command::Migrate` runs `repo.migrate()` even when the top-level `--no-migrate` flag is present.
    - `--no-migrate` still suppresses startup migrations for `Init`, `Create`, `Read`, `Query`, and `Update`.
    - Unit tests cover the migration-decision helper so the behavior is locked without needing a live database for every branch.
  </behavior>
  <acceptance_criteria>
    - `crates/cli/src/main.rs` contains a helper or equivalent branch logic that distinguishes the `Migrate` subcommand from startup migration gating.
    - `crates/cli/src/main.rs` includes tests that assert the `Migrate` path is not blocked by `--no-migrate`.
    - Existing CLI JSON input/output handling remains unchanged for non-migrate commands.
  </acceptance_criteria>
  <action>Refactor the CLI startup flow so the top-level migration gate applies to ordinary commands only, not the `Migrate` subcommand. Add focused unit tests around the decision logic (or an extracted helper) that prove `Migrate` still invokes migrations while the other commands continue honoring `--no-migrate`.</action>
  <verify><automated>cargo test -p docracy-cli</automated></verify>
  <done>`docracy --no-migrate migrate` no longer reports success without applying migrations, and the ordinary commands still obey the startup flag.</done>
</task>

<task type="auto">
  <name>Task 3: Align the README with the shipped contract</name>
  <files>README.md</files>
  <read_first>README.md, crates/cli/src/main.rs, crates/core/src/query.rs</read_first>
  <acceptance_criteria>
    - README examples show `Read` input as `{ "ids": [...] }` only.
    - README update examples use `expected_revision` and no longer imply a different update field name.
    - README explicitly says `extensions.*` search is deferred/unsupported in v1.
  </acceptance_criteria>
  <action>Rewrite the user-facing examples so they match the current CLI and query behavior exactly: keep the `Read` payload to `ids`, keep update examples on `expected_revision`, and make the deferred `extensions` search note explicit so readers do not copy a contract the implementation rejects.</action>
  <verify><automated>grep -nE 'expected_revision|"ids"|extensions\..*deferred' README.md</automated></verify>
  <done>The documentation matches the shipped API surface and no longer advertises unsupported extension-search behavior.</done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-postgres --test postgres_integration` passes.
- `cargo test -p docracy-cli` passes.
- README grep checks confirm the contract text was updated.
</verification>

<success_criteria>
- The phase closes the remaining stabilization gaps without introducing new feature scope.
- The Postgres layer rejects malformed revision graphs and preserves stable cursor pagination.
- The CLI migrate path behaves correctly even when `--no-migrate` is set.
- The README no longer conflicts with the implemented read/query/update contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-stabilization-gap-closure/03-01-SUMMARY.md`
</output>
