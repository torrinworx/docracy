---
phase: 12-vector-mirror-helper-and-vector-query-support
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - crates/postgres/Cargo.toml
  - Cargo.lock
  - crates/postgres/src/vector.rs
  - crates/postgres/src/lib.rs
  - crates/core/src/query.rs
  - crates/core/src/repository.rs
  - crates/core/src/service.rs
  - crates/core/src/lib.rs
  - crates/postgres/tests/postgres_integration.rs
  - README.md
autonomous: true
requirements:
  - VEC-01

must_haves:
  truths:
    - "Qdrant receives workspace-scoped upserts that replace older embeddings for the same document instead of accumulating stale vectors."
    - "Vector search returns only the active workspace's current documents, with archived/deleted rows filtered the same way Postgres does."
    - "Hydration from Postgres remains the final check, so Qdrant never becomes the source of truth."
  artifacts:
    - path: "crates/postgres/src/vector.rs"
      provides: "Qdrant dispatch and mirror queue flush"
      contains: "QDRANT_URL"
    - path: "crates/core/src/query.rs"
      provides: "Vector query input contract"
      contains: "VectorQueryInput"
    - path: "crates/core/src/repository.rs"
      provides: "Vector search repository hook"
      contains: "query_vector_documents"
  key_links:
    - from: "crates/core/src/query.rs"
      to: "crates/core/src/repository.rs"
      via: "vector query dispatch"
      pattern: "VectorQueryInput"
    - from: "crates/postgres/src/vector.rs"
      to: "Qdrant collection names"
      via: "workspace-scoped collection naming"
      pattern: "docracy_workspace_"
    - from: "crates/postgres/src/vector.rs"
      to: "crates/postgres/tests/postgres_integration.rs"
      via: "archive/workspace regression coverage"
      pattern: "vector_mirror"
---

<objective>
Flush the current mirror queue into Qdrant and add vector-query support that stays scoped to the current workspace and archive state.

Purpose: make derived vector retrieval usable without letting Qdrant outrank the canonical Postgres document store.
Output: Qdrant mirror dispatch, vector query routing, and regression coverage for stale/archive/workspace behavior.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/codebase/INTEGRATIONS.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation/08-01-SUMMARY.md
@.planning/phases/08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation/08-02-SUMMARY.md
@crates/core/src/vector.rs
@crates/core/src/query.rs
@crates/core/src/repository.rs
@crates/core/src/service.rs
@crates/core/src/lib.rs
@crates/postgres/src/lib.rs
@crates/postgres/src/vector.rs
@crates/postgres/tests/postgres_integration.rs
@README.md
@compose.yml

<interfaces>
From `crates/core/src/vector.rs`:
```rust
pub struct VectorMirrorRecord {
    pub workspace_id: Uuid,
    pub document_id: DocumentId,
    pub revision_id: RevisionId,
    pub archived_at: Option<DateTime<Utc>>,
    pub deleted_at: Option<DateTime<Utc>>,
    pub embedding: Vec<f32>,
}

pub struct VectorQueryInput {
    pub query: DocumentQuery,
    pub embedding: Vec<f32>,
}
```

From `crates/core/src/repository.rs` (planned vector hook shape):
```rust
async fn query_vector_documents(
    &self,
    query: DocumentQuery,
    embedding: Vec<f32>,
) -> Result<DocumentQueryResult, RepoError>;
```

From `crates/postgres/src/lib.rs`:
```rust
pub struct PgRepository {
    pool: PgPool,
    workspace_id: Option<WorkspaceUuid>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flush the vector mirror queue into workspace-scoped Qdrant collections</name>
  <files>crates/postgres/Cargo.toml, Cargo.lock, crates/postgres/src/vector.rs, crates/postgres/src/lib.rs</files>
  <read_first>
    <file>crates/postgres/src/lib.rs</file>
    <file>crates/postgres/src/vector.rs</file>
    <file>crates/postgres/Cargo.toml</file>
    <file>migrations/0007_vector_mirror_queue.sql</file>
    <file>compose.yml</file>
    <file>crates/postgres/tests/postgres_integration.rs</file>
  </read_first>
  <behavior>
    - Qdrant collection names are workspace-scoped, so one workspace cannot query another workspace's embeddings.
    - The first flushed embedding defines the collection dimension; later rows must match that dimension.
    - Mirror rows stay pending if Qdrant is unavailable, so Postgres remains canonical.
  </behavior>
  <action>
    Add a Qdrant client helper in `crates/postgres/src/vector.rs` using `reqwest` and the REST API. Read `QDRANT_URL` when set and fall back to `http://127.0.0.1:6333`. Create a collection on demand with cosine distance, upsert points keyed by document id, and store the current workspace/document/revision/archive metadata in the payload. Use `docracy_workspace_{workspace_id}` as the collection name, including the reserved nil UUID for the global/shared scope. After a successful upsert, mark the matching queue row processed; if Qdrant is down or the embedding dimension mismatches, leave the queue row pending and return a storage error. Keep the queue current-only: a newer snapshot should replace the older row before the next flush.
  </action>
  <acceptance_criteria>
    - `crates/postgres/src/vector.rs` contains `QDRANT_URL` handling and a `docracy_workspace_` collection naming helper.
    - Qdrant upserts overwrite the point for the same document id.
    - Queue rows remain pending when the flush fails.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres --test postgres_integration vector_dispatch</automated>
  </verify>
  <done>
    The mirror queue can be flushed into per-workspace Qdrant collections without letting Qdrant outrank Postgres.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add vector query routing and Postgres hydration</name>
  <files>crates/core/src/query.rs, crates/core/src/repository.rs, crates/core/src/service.rs, crates/core/src/lib.rs, crates/postgres/src/lib.rs</files>
  <read_first>
    <file>crates/core/src/query.rs</file>
    <file>crates/core/src/repository.rs</file>
    <file>crates/core/src/service.rs</file>
    <file>crates/core/src/lib.rs</file>
    <file>crates/core/src/vector.rs</file>
    <file>crates/postgres/src/lib.rs</file>
    <file>crates/postgres/src/vector.rs</file>
  </read_first>
  <behavior>
    - Raw SQL still takes precedence over everything else.
    - Vector search reuses the same document filters and defaults as guided search, so archived/deleted behavior stays identical.
    - Qdrant only ranks candidate ids; Postgres remains the final source of truth for the returned documents.
  </behavior>
  <action>
    Extend `QueryInput` with a top-level `embedding` field and route it to a new vector path when present. Update `crates/core/src/query.rs` to build a `VectorQueryInput` that reuses the existing `DocumentQuery` filter defaults plus the embedding payload. Extend `crates/core/src/repository.rs` with a `query_vector_documents` hook that defaults to an unsupported-storage error. In `crates/core/src/service.rs`, dispatch vector requests to that hook, then hydrate the returned ids from Postgres in the same ranked order before projecting rows. In `crates/postgres/src/lib.rs`, implement the hook by searching the workspace-scoped Qdrant collection, filtering out documents that are no longer active in Postgres, and preserving the qdrant ranking order in the hydrated response.
  </action>
  <acceptance_criteria>
    - `QueryInput` accepts an `embedding` field and routes to vector mode.
    - `Repository` has a vector-search hook with a safe default error.
    - Vector results are hydrated from Postgres, not returned directly from Qdrant.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-core --lib query</automated>
  </verify>
  <done>
    The core can ask Qdrant for ranked ids while Postgres stays canonical for the returned documents.</done>
</task>

<task type="auto">
  <name>Task 3: Prove archive/workspace isolation and document the derived vector contract</name>
  <files>crates/postgres/tests/postgres_integration.rs, README.md</files>
  <read_first>
    <file>crates/postgres/tests/postgres_integration.rs</file>
    <file>crates/postgres/src/vector.rs</file>
    <file>README.md</file>
    <file>crates/core/src/query.rs</file>
  </read_first>
  <action>
    Add integration coverage that proves workspace A cannot see workspace B, archived documents drop out of active vector queries, and a later write replaces the older embedding instead of leaving stale data behind. Update the root README to state the key contract plainly: Postgres is canonical, Qdrant mirrors only the current workspace-scoped snapshot, and archive/deleted state changes must be reflected in both stores.
  </action>
  <acceptance_criteria>
    - There is an integration test for workspace isolation in vector search.
    - There is an integration test for archived documents disappearing from active vector search.
    - The README mentions Postgres as the source of truth and Qdrant as a mirrored derived index.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres --test postgres_integration vector</automated>
  </verify>
  <done>
    The phase has end-to-end proof that workspace scope and archive state stay aligned across Postgres and Qdrant.</done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-core --lib query` passes.
- `cargo test -p docracy-postgres --test postgres_integration vector` passes.
- Qdrant results are always filtered through current Postgres state before being returned.
</verification>

<success_criteria>
- Vector mirror updates stay workspace-scoped, archive-aware, and subordinate to Postgres.
</success_criteria>

<output>
After completion, create `.planning/phases/12-vector-mirror-helper-and-vector-query-support/12-02-SUMMARY.md`
</output>
