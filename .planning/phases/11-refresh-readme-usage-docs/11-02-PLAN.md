---
phase: 11-refresh-readme-usage-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/mcp/README.md
autonomous: true
requirements: ["DOC-01"]

must_haves:
  truths:
    - "MCP README reflects the current startup contract for workspace binding and task scoping"
    - "MCP README examples match the shipped tool payloads and alias behavior"
    - "MCP README does not imply unsupported transport or tool changes"
  artifacts:
    - path: "crates/mcp/README.md"
      provides: "MCP contract and runtime usage docs"
      contains: "task_context_documents"
    - path: "crates/mcp/README.md"
      provides: "Tool payload examples and error contract"
      contains: "expected_revision"
  key_links:
    - from: "crates/mcp/README.md"
      to: "crates/mcp/src/tools.rs"
      via: "tool JSON examples"
      pattern: "expected_revision"
    - from: "crates/mcp/README.md"
      to: "crates/mcp/src/config.rs"
      via: "startup env contract"
      pattern: "DOCRACY_TASK_SCOPE"
---

<objective>
Refresh `crates/mcp/README.md` so the MCP runtime contract, tool payload examples, and startup env guidance match the shipped implementation.

Purpose: Keep the interface-level docs accurate for MCP client authors and maintainers.
Output: `crates/mcp/README.md` documents current binding, task scoping, and error semantics.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@crates/mcp/README.md
@crates/mcp/src/config.rs
@crates/mcp/src/runtime.rs
@crates/mcp/src/tools.rs
@crates/mcp/src/operations.rs
@crates/mcp/src/bin/docracy-mcp.rs
@.planning/phases/10-task-scoped-init-contexts/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update workspace binding and task-scope docs</name>
  <files>crates/mcp/README.md</files>
  <read_first>
    <file>crates/mcp/README.md</file>
    <file>crates/mcp/src/config.rs</file>
    <file>crates/mcp/src/runtime.rs</file>
    <file>.planning/phases/10-task-scoped-init-contexts/10-03-SUMMARY.md</file>
  </read_first>
  <action>
    Rewrite the opening section and `init` example so they match the actual startup behavior:
    - `WORKSPACE_ID` binds the process for the full MCP session
    - omitted `WORKSPACE_ID` stays on the shared/global path
    - `DOCRACY_TASK_SCOPE` is optional and additive
    - `context_documents` remains the full active set
    - `task_context_documents` is filtered via `extensions.task_scopes`
    Keep the JSON examples concrete and aligned with the shipped field names.
  </action>
  <acceptance_criteria>
    <criterion>`crates/mcp/README.md` contains `WORKSPACE_ID` and `DOCRACY_TASK_SCOPE`.</criterion>
    <criterion>`crates/mcp/README.md` contains `context_documents` and `task_context_documents`.</criterion>
    <criterion>`crates/mcp/README.md` contains `extensions.task_scopes` and the phrase `full active set`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('crates/mcp/README.md').read_text()
assert 'WORKSPACE_ID' in text
assert 'DOCRACY_TASK_SCOPE' in text
assert 'context_documents' in text
assert 'task_context_documents' in text
assert 'extensions.task_scopes' in text
assert 'full active set' in text
PY</automated>
  </verify>
  <done>
    MCP README explains the current session binding and task-scoping contract.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh tool contract and error/alignment notes</name>
  <files>crates/mcp/README.md</files>
  <read_first>
    <file>crates/mcp/README.md</file>
    <file>crates/mcp/src/tools.rs</file>
    <file>crates/mcp/src/operations.rs</file>
    <file>crates/mcp/src/bin/docracy-mcp.rs</file>
  </read_first>
  <action>
    Rewrite the `Tools`, `Alignment With CLI`, and `Error Contract` sections so the examples match the shipped contract exactly:
    - `expected_revision` is the canonical update field
    - `expected_head` is documented only as an alias
    - query docs call out that raw SQL takes precedence when `sql` is present
    - failures are described as structured `ErrorData` with machine-readable `kind` values
    Keep the tool names stable and avoid implying unsupported transport behavior or new tool surface area.
  </action>
  <acceptance_criteria>
    <criterion>`crates/mcp/README.md` contains `expected_revision` and `expected_head`.</criterion>
    <criterion>`crates/mcp/README.md` contains `raw SQL runs read-only`.</criterion>
    <criterion>`crates/mcp/README.md` contains `ErrorData` and `kind`.</criterion>
    <criterion>`crates/mcp/README.md` states that `sql` takes precedence over `query`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('crates/mcp/README.md').read_text()
assert 'expected_revision' in text
assert 'expected_head' in text
assert 'raw SQL runs read-only' in text
assert 'ErrorData' in text
assert 'kind' in text
assert 'sql' in text and 'query' in text and 'takes precedence' in text
PY</automated>
  </verify>
  <done>
    The MCP README accurately documents the current tool contract and error semantics.
  </done>
</task>

</tasks>

<verification>
- `crates/mcp/README.md` matches the current runtime/config and tool payload contract.
- The README stays aligned with the shipped implementation without promising extra transports or tools.
</verification>

<success_criteria>
- MCP-facing documentation is current, concrete, and consistent with the runtime code.
</success_criteria>

<output>
After completion, create `.planning/phases/11-refresh-readme-usage-docs/11-02-SUMMARY.md`
</output>
