---
phase: 11-refresh-readme-usage-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: ["DOC-01"]

must_haves:
  truths:
    - "README explains the current shipped usage surface without presenting v1.0 wording as the active contract"
    - "README shows task-scoped init and workspace bootstrap as additive, current behavior"
    - "README keeps the agent startup guidance aligned with the repo's required bootstrapping flow"
  artifacts:
    - path: "README.md"
      provides: "Root usage docs and current-state summary"
      contains: "task_context_documents"
    - path: "README.md"
      provides: "Workspace bootstrap walkthrough"
      contains: "docracy workspace create"
  key_links:
    - from: "README.md"
      to: "crates/cli/src/main.rs"
      via: "workspace-create CLI docs and UUID handoff"
      pattern: "docracy workspace create"
    - from: "README.md"
      to: "crates/mcp/src/bin/docracy-mcp.rs"
      via: "MCP startup/env guidance"
      pattern: "DOCRACY_TASK_SCOPE"
---

<objective>
Refresh the root README so the current-state and usage docs match the shipped v1.1 contract and task-scoped init/workspace bootstrap flow.

Purpose: Keep the primary entrypoint docs accurate for operators and agents using Docracy locally.
Output: `README.md` reflects the current contract, startup env vars, and bootstrap steps.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@crates/cli/src/main.rs
@crates/mcp/src/bin/docracy-mcp.rs
@.planning/phases/09-cli-workspace-create-command/09-01-SUMMARY.md
@.planning/phases/10-task-scoped-init-contexts/10-02-SUMMARY.md
@.planning/phases/10-task-scoped-init-contexts/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite current-state and contract sections</name>
  <files>README.md</files>
  <read_first>
    <file>README.md</file>
    <file>.planning/phases/09-cli-workspace-create-command/09-01-SUMMARY.md</file>
    <file>.planning/phases/10-task-scoped-init-contexts/10-02-SUMMARY.md</file>
    <file>.planning/phases/10-task-scoped-init-contexts/10-03-SUMMARY.md</file>
  </read_first>
  <action>
    Replace the top "Current state", "Tools/endpoints", "Document types", and "Document extensibility" prose so it describes the current shipped contract exactly:
    - call the current milestone "v1.1 MCP Server Interface"
    - keep `Init/Create/Read/Query/Update` as the shipped tools
    - describe `task_scope` + `task_context_documents` as additive init fields, not a replacement for `context_documents`
    - document workspace bootstrap as CLI-only via `docracy workspace create` and `WORKSPACE_ID`
    - keep extension-field search deferred/unsupported in v1
    - do not present future ideas as current behavior
  </action>
  <acceptance_criteria>
    <criterion>README.md contains `v1.1 MCP Server Interface`.</criterion>
    <criterion>README.md contains `task_context_documents` and `DOCRACY_TASK_SCOPE`.</criterion>
    <criterion>README.md contains `docracy workspace create` and `WORKSPACE_ID`.</criterion>
    <criterion>README.md no longer starts the current-state lead-in with `Docracy v1.0 is the Postgres-backed document store`.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('README.md').read_text()
assert 'v1.1 MCP Server Interface' in text
assert 'task_context_documents' in text
assert 'DOCRACY_TASK_SCOPE' in text
assert 'docracy workspace create' in text
assert 'WORKSPACE_ID' in text
assert 'Docracy v1.0 is the Postgres-backed document store' not in text
PY</automated>
  </verify>
  <done>
    The README accurately describes the current contract without stale v1.0 wording.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refresh usage walkthrough and agent bootstrap snippet</name>
  <files>README.md</files>
  <read_first>
    <file>README.md</file>
    <file>crates/cli/src/main.rs</file>
    <file>crates/mcp/src/bin/docracy-mcp.rs</file>
    <file>.planning/phases/09-cli-workspace-create-command/09-01-SUMMARY.md</file>
  </read_first>
  <action>
    Update the "How to use" section so the OpenCode MCP configuration example, the workspace bootstrap walkthrough, and the AGENTS.md snippet stay aligned with the current startup flow:
    - keep the MCP command invocation concrete and unchanged if it still matches the current binary entrypoint
    - show the `workspace create` -> `WORKSPACE_ID` handoff clearly
    - mention `DOCRACY_WORKSPACE_ID` and `DOCRACY_TASK_SCOPE` if a client-env example is shown
    - preserve the exact AGENTS.md reminder block so readers can copy/paste it
  </action>
  <acceptance_criteria>
    <criterion>README.md contains `DOCRACY_WORKSPACE_ID`.</criterion>
    <criterion>README.md contains `DOCRACY_TASK_SCOPE` and `WORKSPACE_ID` in the usage section.</criterion>
    <criterion>README.md contains the exact reminder sentence `Before responding to the user or conducting a task, run the docracy_init tool call.`</criterion>
  </acceptance_criteria>
  <verify>
    <automated>python - &lt;&lt;'PY'
from pathlib import Path
text = Path('README.md').read_text()
assert 'DOCRACY_WORKSPACE_ID' in text
assert 'DOCRACY_TASK_SCOPE' in text
assert 'WORKSPACE_ID' in text
assert 'Before responding to the user or conducting a task, run the docracy_init tool call.' in text
PY</automated>
  </verify>
  <done>
    The root README gives a copy-pasteable, current usage walkthrough for local operators and agents.
  </done>
</task>

</tasks>

<verification>
- `README.md` matches the shipped task-scoped init and workspace bootstrap contract.
- The README no longer reads like a v1.0-only document.
</verification>

<success_criteria>
- The root README accurately explains how to run Docracy today without stale wording or missing env vars.
</success_criteria>

<output>
After completion, create `.planning/phases/11-refresh-readme-usage-docs/11-01-SUMMARY.md`
</output>
