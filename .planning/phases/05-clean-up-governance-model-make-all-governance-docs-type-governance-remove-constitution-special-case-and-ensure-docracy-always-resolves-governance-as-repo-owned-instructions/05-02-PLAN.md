---
phase: 05-clean-up-governance-model-make-all-governance-docs-type-governance-remove-constitution-special-case-and-ensure-docracy-always-resolves-governance-as-repo-owned-instructions
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - crates/core/src/governance.rs
  - crates/cli/src/main.rs
  - crates/mcp/src/config.rs
  - crates/mcp/src/runtime.rs
  - crates/mcp/src/bin/docracy-mcp.rs
  - README.md
  - crates/mcp/README.md
autonomous: true
requirements:
  - GOV-07
  - DOC-03
must_haves:
  truths:
    - "CLI and MCP startup always load the repo-owned `./governance` bundle."
    - "No startup surface accepts an arbitrary `governance_dir` override anymore."
    - "Public docs describe the governance document type and the fixed repo-owned path."
  artifacts:
    - path: crates/core/src/governance.rs
      provides: "repo-owned governance path helper"
    - path: crates/cli/src/main.rs
      provides: "Init command wired to the fixed governance bundle"
    - path: crates/mcp/src/runtime.rs
      provides: "MCP bootstrap that uses the same fixed bundle path"
    - path: README.md
      provides: "user-facing contract for fixed governance path and governance doc type"
    - path: crates/mcp/README.md
      provides: "MCP startup and tool contract notes"
  key_links:
    - from: crates/core/src/governance.rs
      to: crates/cli/src/main.rs
      via: "shared repo-owned governance helper"
      pattern: "./governance|repo_owned"
    - from: crates/core/src/governance.rs
      to: crates/mcp/src/runtime.rs
      via: "shared repo-owned governance helper"
      pattern: "./governance|repo_owned"
    - from: README.md
      to: crates/mcp/README.md
      via: "consistent startup guidance"
      pattern: "./governance|governance_dir"
---

<objective>
Lock the repo-owned governance bundle path into the interface layer and refresh the public contract text.

Purpose: keep Docracy’s startup story simple and repo-defined, so local and MCP launches always use the same governance instructions without advertising an arbitrary path.
Output: a fixed governance path helper, CLI/MCP startup updates, and docs that match the new contract.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
From `crates/core/src/governance.rs`:
```rust
pub struct FsGovernanceSource {
    dir: PathBuf,
}
impl FsGovernanceSource {
    pub fn new(dir: impl Into<PathBuf>) -> Self;
}
pub trait GovernanceSource {
    fn load_bundle(&self) -> Result<GovernanceBundle, GovernanceError>;
}
```

From `crates/cli/src/main.rs`:
```rust
enum Command {
    Init {
        governance_dir: PathBuf,
    },
}
```

From `crates/mcp/src/config.rs`:
```rust
pub struct McpStartupConfig {
    pub database_url: String,
    pub governance_dir: PathBuf,
    pub run_migrations: bool,
    pub transport: McpTransport,
}
```
</interfaces>

@crates/core/src/governance.rs
@crates/cli/src/main.rs
@crates/mcp/src/config.rs
@crates/mcp/src/runtime.rs
@crates/mcp/src/bin/docracy-mcp.rs
@README.md
@crates/mcp/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze CLI and MCP startup on the repo-owned governance bundle</name>
  <files>crates/core/src/governance.rs, crates/cli/src/main.rs, crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/bin/docracy-mcp.rs</files>
  <read_first>crates/core/src/governance.rs, crates/cli/src/main.rs, crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/bin/docracy-mcp.rs</read_first>
  <behavior>
    - `Init` and MCP startup always point at the fixed repo-owned `./governance` bundle.
    - `governance_dir` disappears from the user-facing startup surface.
    - Existing migration toggles, transport selection, and stdout/stderr rules stay unchanged.
  </behavior>
  <action>Add a core helper for the repo-owned governance path (for example `FsGovernanceSource::repo_owned()` or an equivalent constant-backed constructor), remove `governance_dir` from CLI `Init` and `McpStartupConfig`, and wire both binaries to the fixed `./governance` bundle. Keep the current migration toggle and transport selection behavior intact while the governance path stops being user-configurable.</action>
  <verify>cargo test -p docracy-core governance && cargo test -p docracy-cli should_run_startup_migrations && cargo test -p docracy-mcp --lib</verify>
  <acceptance_criteria>
    - CLI `Init` no longer accepts a `governance_dir` argument.
    - MCP startup config no longer carries a `governance_dir` field.
    - Core exposes a fixed repo-owned governance path helper used by both entrypoints.
  </acceptance_criteria>
  <done>All startup surfaces resolve the same repo-owned governance bundle path.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh public docs for the governance type and fixed path</name>
  <files>README.md, crates/mcp/README.md</files>
  <read_first>README.md, crates/mcp/README.md</read_first>
  <action>Update the user-facing contract text so it names the `governance` document type, describes `./governance` as the fixed repo-owned bundle path, and removes any wording that suggests users can point Docracy at an arbitrary governance directory. Keep the rest of the shipped CLI/MCP JSON examples aligned with the current contract.</action>
  <verify>node -e "const fs=require('fs'); const readme=fs.readFileSync('README.md','utf8'); const mcp=fs.readFileSync('crates/mcp/README.md','utf8'); if (!readme.includes('./governance')) throw new Error('README missing ./governance'); if (!mcp.includes('./governance')) throw new Error('MCP README missing ./governance'); if (/governance_dir/.test(readme) || /governance_dir/.test(mcp)) throw new Error('docs still advertise governance_dir'); if (!/governance/i.test(readme)) throw new Error('README missing governance wording');"</verify>
  <acceptance_criteria>
    - `README.md` describes the governance doc type and fixed repo-owned path.
    - `crates/mcp/README.md` no longer advertises `governance_dir` as startup input.
    - The docs still show the shipped JSON tool/CLI contract shapes.
  </acceptance_criteria>
  <done>Public docs match the repo-owned governance startup story.</done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-core governance` passes.
- `cargo test -p docracy-cli should_run_startup_migrations` passes.
- `cargo test -p docracy-mcp --lib` passes.
- `README.md` and `crates/mcp/README.md` both mention `./governance` and no longer advertise `governance_dir`.
</verification>

<success_criteria>
- Startup is repo-defined and no longer user-configurable by path.
- The CLI and MCP share the same fixed governance bundle path.
- Public docs reflect the new governance contract without ambiguity.
</success_criteria>

<output>
After completion, create `.planning/phases/05-clean-up-governance-model-make-all-governance-docs-type-governance-remove-constitution-special-case-and-ensure-docracy-always-resolves-governance-as-repo-owned-instructions/05-02-SUMMARY.md`
</output>
