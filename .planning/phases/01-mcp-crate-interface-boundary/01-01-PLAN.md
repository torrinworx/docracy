---
phase: 01-mcp-crate-interface-boundary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/mcp/Cargo.toml
  - crates/mcp/src/lib.rs
  - crates/mcp/src/config.rs
  - crates/mcp/src/runtime.rs
  - .planning/codebase/ARCHITECTURE.md
autonomous: true
requirements:
  - IFC-01
  - CFG-01
  - DOC-02
must_haves:
  truths:
    - The workspace includes a dedicated `crates/mcp` crate alongside the existing CLI crate.
    - MCP runtime configuration is defined in the interface crate and covers database URL, governance path, migration behavior, and transport selection.
    - Contributor-facing architecture notes explicitly say the MCP crate is a thin interface over `docracy_core`.
  artifacts:
    - path: Cargo.toml
      provides: Workspace membership for the new MCP crate
      contains: crates/mcp
    - path: crates/mcp/Cargo.toml
      provides: Dedicated MCP interface crate manifest
      contains: name = "docracy-mcp"
    - path: crates/mcp/src/config.rs
      provides: MCP-owned startup configuration model
      contains: database_url
    - path: crates/mcp/src/runtime.rs
      provides: Transport-agnostic runtime/bootstrap helper
      contains: run_migrations
    - path: .planning/codebase/ARCHITECTURE.md
      provides: Updated interface boundary guidance for contributors
      contains: crates/mcp
  key_links:
    - from: crates/mcp/src/runtime.rs
      to: crates/postgres/src/lib.rs
      via: shared repository bootstrap using the existing Postgres adapter
      pattern: PgRepository
    - from: .planning/codebase/ARCHITECTURE.md
      to: crates/core/src/service.rs
      via: documented boundary stating business rules stay in the core
      pattern: create_document|init_bundle|query_documents|read_documents|update_document
---

<objective>
Bootstrap the dedicated MCP crate and its reusable runtime/config boundary.

Purpose: make phase 2 and phase 3 transport work build on one interface crate with one startup path instead of re-creating setup per transport.
Output: a new `crates/mcp` crate, reusable startup/config modules, and an architecture note that documents the intended core-vs-interface boundary.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-mcp-crate-interface-boundary/01-CONTEXT.md
@.planning/phases/01-mcp-crate-interface-boundary/01-RESEARCH.md

@Cargo.toml
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@crates/core/src/lib.rs
@crates/core/src/governance.rs
@crates/postgres/src/lib.rs
@crates/cli/src/main.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the workspace-level MCP crate scaffold</name>
  <files>Cargo.toml, crates/mcp/Cargo.toml, crates/mcp/src/lib.rs</files>
  <read_first>Cargo.toml, crates/core/Cargo.toml, crates/postgres/Cargo.toml, crates/cli/Cargo.toml</read_first>
  <action>Update the workspace root so `crates/mcp` is a first-class member, then create `crates/mcp/Cargo.toml` and `crates/mcp/src/lib.rs` as the dedicated interface crate skeleton. Keep the crate explicitly focused on MCP/interface concerns; do not move business logic or repository implementations into it. Reuse the workspace's current dependency style and package naming convention by naming the package `docracy-mcp` and exposing only the modules phase 1 actually needs.</action>
  <verify><automated>cargo check -p docracy-mcp</automated></verify>
  <acceptance_criteria>
    - Root `Cargo.toml` lists `crates/mcp` in `members`.
    - `crates/mcp/Cargo.toml` declares the `docracy-mcp` package with the dependencies needed for config/runtime/bootstrap work.
    - `crates/mcp/src/lib.rs` exists and exposes the phase 1 modules without duplicating core business APIs.
  </acceptance_criteria>
  <done>The repository builds with a new MCP interface crate in the workspace.</done>
</task>

<task type="auto">
  <name>Task 2: Define MCP-owned startup config and reusable runtime bootstrap</name>
  <files>crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/lib.rs</files>
  <read_first>crates/postgres/src/lib.rs, crates/core/src/governance.rs, crates/cli/src/main.rs</read_first>
  <action>Create a config module and a runtime/bootstrap module in `crates/mcp` that keep startup concerns out of future transport handlers. The config model should include concrete fields for `database_url`, `governance_dir`, whether to run migrations at startup, and a transport selection enum that can represent both `stdio` and `http`. The runtime helper should centralize creation of `PgRepository`, `FsGovernanceSource`, `SystemClock`, and `UuidV4Generator`, and it should expose an initialization path that future stdio and HTTP entrypoints can both call without re-copying setup logic.</action>
  <verify><automated>cargo check -p docracy-mcp</automated></verify>
  <acceptance_criteria>
    - `crates/mcp/src/config.rs` defines startup config fields for database URL, governance path, migration behavior, and transport selection.
    - `crates/mcp/src/runtime.rs` wires the existing Postgres adapter and governance source into a reusable startup helper.
    - No transport-specific request handling or business logic is embedded in the runtime/bootstrap code.
  </acceptance_criteria>
  <done>The MCP crate owns startup/runtime concerns in one reusable place.</done>
</task>

<task type="auto">
  <name>Task 3: Document the MCP interface boundary for contributors</name>
  <files>.planning/codebase/ARCHITECTURE.md</files>
  <read_first>.planning/codebase/ARCHITECTURE.md, .planning/PROJECT.md, .planning/research/MCP_SERVER.md</read_first>
  <action>Update `.planning/codebase/ARCHITECTURE.md` so it includes `crates/mcp` as a new interface/driver layer and states explicitly that Create/Read/Query/Update/Init business rules stay in `docracy_core`. Make the note concrete enough that future agents can see where runtime/bootstrap code ends and where domain logic must remain. Keep the wording aligned with the existing architecture analysis style instead of adding a brand-new document format.</action>
  <verify><automated>rg -n "crates/mcp|business rules stay in `docracy_core`|interface crate" .planning/codebase/ARCHITECTURE.md</automated></verify>
  <acceptance_criteria>
    - `.planning/codebase/ARCHITECTURE.md` names `crates/mcp` and describes its role.
    - The architecture note says the MCP crate delegates to `docracy_core` use-cases rather than reimplementing them.
    - The note distinguishes reusable runtime/config code from domain/business logic.
  </acceptance_criteria>
  <done>Contributor docs make the new interface boundary explicit before deeper MCP work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p docracy-mcp` passes
- [ ] `crates/mcp` exists as a workspace crate with config/runtime modules
- [ ] `.planning/codebase/ARCHITECTURE.md` documents the core-vs-MCP boundary
</verification>

<success_criteria>

- All tasks completed
- The workspace now has a dedicated MCP interface crate
- MCP startup concerns are reusable across future transports
- The contributor docs make it hard to accidentally move business rules out of the core

</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-crate-interface-boundary/01-01-SUMMARY.md`
</output>
