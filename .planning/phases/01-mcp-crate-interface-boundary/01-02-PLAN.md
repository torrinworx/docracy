---
phase: 01-mcp-crate-interface-boundary
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - crates/mcp/src/lib.rs
  - crates/mcp/src/operations.rs
  - crates/mcp/src/error.rs
  - crates/mcp/tests/operations.rs
autonomous: true
requirements:
  - IFC-02
  - IFC-03
must_haves:
  truths:
    - MCP-facing document operations delegate to existing `docracy_core` use-cases.
    - MCP response and error mapping live in the interface crate rather than the core.
    - Revision-conflict and validation failures retain structured machine-readable detail at the interface boundary.
  artifacts:
    - path: crates/mcp/src/operations.rs
      provides: Thin Docracy-to-MCP delegation layer
      contains: update_document
    - path: crates/mcp/src/error.rs
      provides: MCP-facing error translation
      contains: revision_conflict
    - path: crates/mcp/tests/operations.rs
      provides: Regression coverage for delegation and boundary mapping
      contains: query_documents
  key_links:
    - from: crates/mcp/src/operations.rs
      to: crates/core/src/service.rs
      via: thin delegation to exported use-cases
      pattern: init_bundle|create_document|read_documents|query_documents|update_document
    - from: crates/mcp/src/error.rs
      to: crates/core/src/errors.rs
      via: interface-local translation from domain errors to protocol-facing errors
      pattern: CoreError|RepoError
---

<objective>
Create the reusable MCP-facing delegation and error-mapping layer on top of the new crate bootstrap.

Purpose: give later tool registration and transport work a stable Docracy-specific interface boundary without duplicating domain behavior.
Output: thin operation helpers that call the core, stable MCP-facing response/error mapping, and focused tests that lock the boundary before transport code lands.
</objective>

<execution_context>
@/home/torrin/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/torrin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-mcp-crate-interface-boundary/01-CONTEXT.md
@.planning/phases/01-mcp-crate-interface-boundary/01-RESEARCH.md
@.planning/phases/01-mcp-crate-interface-boundary/01-01-PLAN.md

@crates/core/src/lib.rs
@crates/core/src/service.rs
@crates/core/src/errors.rs
@crates/core/src/governance.rs
@crates/postgres/src/lib.rs
@crates/cli/src/main.rs
@crates/mcp/src/lib.rs
@crates/mcp/src/config.rs
@crates/mcp/src/runtime.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the thin MCP operation/delegation layer</name>
  <files>crates/mcp/src/operations.rs, crates/mcp/src/lib.rs</files>
  <read_first>crates/core/src/lib.rs, crates/core/src/service.rs, crates/mcp/src/runtime.rs</read_first>
  <action>Create `crates/mcp/src/operations.rs` as the Docracy-specific interface layer that future MCP tools will call. Define thin operations for init/create/read/query/update that accept MCP-facing inputs or runtime dependencies, then delegate directly to `docracy_core::{init_bundle, create_document, read_documents, query_documents, update_document}`. Keep this layer focused on orchestration and type translation only; do not re-check business invariants that the core already enforces.</action>
  <verify><automated>cargo test -p docracy-mcp --test operations</automated></verify>
  <acceptance_criteria>
    - `crates/mcp/src/operations.rs` exists and exposes operation helpers for all five shipped Docracy actions.
    - The operation helpers call exported core use-cases instead of duplicating document/governance logic.
    - The crate root re-exports only the MCP-facing modules/types that later plans need.
  </acceptance_criteria>
  <done>The MCP crate has one reusable Docracy operation layer for future tool handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Add stable MCP-facing response and error mapping</name>
  <files>crates/mcp/src/error.rs, crates/mcp/src/operations.rs, crates/mcp/src/lib.rs</files>
  <read_first>crates/core/src/errors.rs, crates/cli/src/main.rs, .planning/research/MCP_SERVER.md</read_first>
  <action>Create interface-local response/error mapping that turns core and setup failures into stable MCP-facing kinds and detail objects. Preserve useful machine-readable detail such as revision conflict `expected`/`actual` data and distinguish validation/config/storage failures cleanly. Keep this mapping in `crates/mcp`, not in `docracy_core`, and keep it protocol-facing rather than CLI-specific.</action>
  <verify><automated>cargo test -p docracy-mcp --test operations</automated></verify>
  <acceptance_criteria>
    - `crates/mcp/src/error.rs` defines a stable error-mapping path for core/setup failures.
    - Revision conflicts and similar structured failures keep machine-readable details.
    - No MCP-specific error or response types are added to `docracy_core`.
  </acceptance_criteria>
  <done>The MCP boundary can present stable failures without contaminating the core with protocol concerns.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the delegation boundary with focused crate tests</name>
  <files>crates/mcp/tests/operations.rs</files>
  <read_first>crates/core/src/memory.rs, crates/core/src/service.rs, crates/mcp/src/operations.rs, crates/mcp/src/error.rs</read_first>
  <action>Add focused tests in `crates/mcp/tests/operations.rs` that exercise the new operation layer and error mapping without needing stdio or HTTP transports. Prefer in-memory/core-backed tests for deterministic behavior: cover at least one successful delegation path and one structured failure path (for example, revision conflict or validation failure). The goal is to lock the interface boundary now so phase 2 can build tool registration on top of a proven layer.</action>
  <verify><automated>cargo test -p docracy-mcp --test operations</automated></verify>
  <acceptance_criteria>
    - `crates/mcp/tests/operations.rs` exists and runs without requiring a live transport.
    - Tests cover at least one success path and one structured failure mapping path.
    - The tests prove the MCP layer is delegating to the core rather than implementing parallel business logic.
  </acceptance_criteria>
  <done>The phase 1 MCP boundary is regression-covered before transport work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo test -p docracy-mcp --test operations` passes
- [ ] `crates/mcp/src/operations.rs` delegates to core use-cases for Init/Create/Read/Query/Update
- [ ] `crates/mcp/src/error.rs` preserves structured failure detail for machine clients
</verification>

<success_criteria>

- All tasks completed
- MCP-facing operations are thin adapters over the shipped core contract
- Error/response mapping is interface-local and machine-readable
- Phase 2 can add tool schemas/registration without redesigning the Docracy boundary

</success_criteria>

<output>
After completion, create `.planning/phases/01-mcp-crate-interface-boundary/01-02-SUMMARY.md`
</output>
