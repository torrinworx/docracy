---
phase: 08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/0004_workspace_tenancy.sql
  - crates/postgres/src/lib.rs
  - crates/postgres/tests/postgres_integration.rs
autonomous: true
requirements:
  - WS-01
  - WS-03
must_haves:
  truths:
    - "Global governance rows remain visible while workspace rows stay isolated across sessions with different bindings."
    - "Every document and revision row carries workspace scope and Postgres RLS blocks cross-workspace reads and writes."
    - "Raw SQL queries obey the same workspace boundary as normal repository calls."
  artifacts:
    - path: "migrations/0004_workspace_tenancy.sql"
      provides: "Workspace table, workspace_id columns, RLS policies, and tenant indexes"
    - path: "crates/postgres/src/lib.rs"
      provides: "Workspace-scoped Postgres connection helper"
    - path: "crates/postgres/tests/postgres_integration.rs"
      provides: "Workspace isolation regression coverage"
  key_links:
    - from: "PgRepository::connect_scoped"
      to: "SET LOCAL docracy.workspace_id"
      via: "session-scoped custom GUC"
    - from: "documents.workspace_id / document_revisions.workspace_id"
      to: "RLS policy USING/WITH CHECK"
      via: "current_setting('docracy.workspace_id', true)"
    - from: "raw SQL execution"
      to: "workspace filters"
      via: "the same RLS policy applies to raw queries"
---

<objective>
Add the database tenant boundary that everything else can bind to, without breaking the existing global governance flow.

Purpose: raw reads and writes must fail closed across workspace boundaries before any MCP session-binding work is wired.
Output: workspace-aware schema, RLS policy, and adapter/test support.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/research/08-RESEARCH.md
@migrations/0001_documents_and_revisions.sql
@migrations/0002_single_constitution.sql
@migrations/0003_content_search.sql
@crates/postgres/src/lib.rs
@crates/postgres/tests/postgres_integration.rs

<interfaces>
From `crates/postgres/src/lib.rs` (current surface):
```rust
pub struct PgRepository;
pub async fn connect(database_url: &str) -> Result<Self, sqlx::Error>;
pub async fn migrate(&self) -> Result<(), sqlx::migrate::MigrateError>;
```

Planned helper for this phase:
```rust
pub async fn connect_scoped(
    database_url: &str,
    workspace_id: Option<uuid::Uuid>,
) -> Result<Self, sqlx::Error>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the workspace tenancy migration and RLS policy</name>
  <files>migrations/0004_workspace_tenancy.sql</files>
  <read_first>migrations/0001_documents_and_revisions.sql, migrations/0002_single_constitution.sql, migrations/0003_content_search.sql, .planning/research/08-RESEARCH.md</read_first>
  <behavior>
    - Create a `workspaces` table with a reserved global workspace row.
    - Add `workspace_id` columns to `documents` and `document_revisions` and backfill existing rows to the global workspace.
    - Add RLS policies that allow the active workspace plus the shared global workspace.
    - Add workspace-leading indexes that match the existing query/order patterns.
  </behavior>
  <action>Create the tenant migration as a forward-only SQLx migration. Add a `workspaces` table with a reserved global UUID row (`00000000-0000-0000-0000-000000000000`) and make `documents.workspace_id` and `document_revisions.workspace_id` non-null with a default that falls back to the global workspace when `current_setting('docracy.workspace_id', true)` is absent. Backfill existing document and revision rows into the global workspace, add indexes with `workspace_id` as the leading key for current lookup/pagination patterns, and enable row-level security on both tables with policies that allow the active workspace plus the global workspace. Do not move any business logic into Rust; this task is schema-only.</action>
  <acceptance_criteria>
    - `migrations/0004_workspace_tenancy.sql` contains a `workspaces` table.
    - The migration contains `workspace_id` columns and a `current_setting('docracy.workspace_id', true)` fallback.
    - The migration enables row-level security and defines policies for both documents and revisions.
    - The migration adds indexes whose leading column is `workspace_id`.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'workspaces\|workspace_id\|current_setting\|ROW LEVEL SECURITY' migrations/0004_workspace_tenancy.sql</automated>
  </verify>
  <done>The schema can represent and enforce workspace scoping without changing the current global governance behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add a workspace-scoped Postgres connection helper and regression tests</name>
  <files>crates/postgres/src/lib.rs, crates/postgres/tests/postgres_integration.rs</files>
  <read_first>crates/postgres/src/lib.rs, crates/postgres/tests/postgres_integration.rs, migrations/0004_workspace_tenancy.sql</read_first>
  <action>Add `PgRepository::connect_scoped(database_url, workspace_id)` so the adapter installs `docracy.workspace_id` on every pooled connection via `after_connect`/session setup while keeping the existing `connect()` path as the shared/global fallback. Then add Postgres integration tests that open two different workspace scopes, write the same document shape into each, and prove the rows do not leak across `read`, `query`, or raw SQL paths. Include an assertion that the shared global workspace still sees governance rows so the existing init flow remains usable.</action>
  <acceptance_criteria>
    - The adapter exposes a workspace-scoped connection helper.
    - Two different workspace bindings cannot see each other's rows.
    - Raw SQL obeys the same workspace boundary as normal repository calls.
    - Global/shared rows remain visible to unbound sessions.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres --test postgres_integration workspace</automated>
  </verify>
  <done>Postgres repository behavior proves workspace isolation end to end.</done>
</task>

</tasks>

<verification>
- `migrations/0004_workspace_tenancy.sql` introduces workspace-aware schema and RLS.
- `cargo test -p docracy-postgres --test postgres_integration workspace` passes.
- The global workspace remains readable so governance/init continues to work.
</verification>

<success_criteria>
- Workspace scope is enforced by the database, not by caller convention.
- Raw SQL, reads, and writes all respect the same workspace boundary.
- Existing global governance behavior still works for shared sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation/08-01-SUMMARY.md`
</output>
