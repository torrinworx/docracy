---
phase: 08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/mcp/src/config.rs
  - crates/mcp/src/runtime.rs
  - crates/mcp/src/bin/docracy-mcp.rs
  - opencode.json
  - crates/mcp/README.md
  - crates/mcp/tests/stdio_binary_smoke.rs
autonomous: true
requirements:
  - WS-02
  - WS-04
must_haves:
  truths:
    - "MCP startup can read an explicit workspace UUID from `WORKSPACE_ID` and keep it in process-wide runtime state."
    - "Project-scoped OpenCode config can pass `WORKSPACE_ID` to the MCP server without filesystem-derived identity heuristics."
    - "The docs explain the workspace-binding contract and preserve the shared/global fallback model."
  artifacts:
    - path: "crates/mcp/src/config.rs"
      provides: "Workspace-aware startup config parsing"
    - path: "crates/mcp/src/runtime.rs"
      provides: "Workspace-bound bootstrap wiring"
    - path: "crates/mcp/src/bin/docracy-mcp.rs"
      provides: "Environment-driven MCP server startup"
    - path: "opencode.json"
      provides: "Project-scoped client config example"
    - path: "crates/mcp/README.md"
      provides: "Workspace-binding documentation"
    - path: "crates/mcp/tests/stdio_binary_smoke.rs"
      provides: "Startup safety regression"
  key_links:
    - from: "opencode.json"
      to: "WORKSPACE_ID"
      via: "project-scoped MCP environment block"
    - from: "McpStartupConfig.workspace_id"
      to: "PgRepository::connect_scoped"
      via: "startup bootstrap passes the bound tenant into the adapter"
    - from: "stdio startup"
      to: "stderr-only setup errors"
      via: "protocol messages remain on stdout"
---

<objective>
Wire the existing MCP interface crate to the new tenant boundary so a project-scoped client can select a workspace explicitly.

Purpose: sessions must arrive scoped before tool handlers run, otherwise the database isolation is just a schema feature without a usable client contract.
Output: workspace-aware MCP startup, local config example, and operator docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@.planning/research/08-RESEARCH.md
@crates/mcp/src/config.rs
@crates/mcp/src/runtime.rs
@crates/mcp/src/bin/docracy-mcp.rs
@crates/mcp/tests/stdio_binary_smoke.rs
@crates/mcp/README.md
@opencode.json

<interfaces>
From `crates/mcp/src/config.rs` (planned surface after this phase):
```rust
pub struct McpStartupConfig {
    pub database_url: String,
    pub run_migrations: bool,
    pub workspace_id: Option<uuid::Uuid>,
    pub transport: McpTransport,
}
```

From `crates/postgres/src/lib.rs` (produced by 08-01):
```rust
pub async fn connect_scoped(
    database_url: &str,
    workspace_id: Option<uuid::Uuid>,
) -> Result<Self, sqlx::Error>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add workspace binding to MCP startup config and bootstrap</name>
  <files>crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/bin/docracy-mcp.rs</files>
  <read_first>crates/mcp/src/config.rs, crates/mcp/src/runtime.rs, crates/mcp/src/bin/docracy-mcp.rs, crates/postgres/src/lib.rs, crates/mcp/tests/stdio_binary_smoke.rs, crates/mcp/tests/operations.rs</read_first>
  <behavior>
    - Parse `WORKSPACE_ID` as a UUID when present.
    - Keep missing `WORKSPACE_ID` as the shared/global fallback path.
    - Preserve the stderr-only setup-error envelope and the existing `DATABASE_URL` behavior.
  </behavior>
  <action>Add `workspace_id: Option<Uuid>` to `McpStartupConfig`, parse `WORKSPACE_ID` from the process environment in `docracy-mcp`, reject malformed UUIDs with the existing setup-error envelope, and thread the binding through `bootstrap()` so it calls `PgRepository::connect_scoped(database_url, workspace_id)`. Keep stdout reserved for protocol messages, preserve `--no-migrate`, and make the global/shared mode work when `WORKSPACE_ID` is absent.</action>
  <acceptance_criteria>
    - The config model carries an optional workspace UUID.
    - Valid UUIDs from `WORKSPACE_ID` are accepted and invalid ones fail fast.
    - The bootstrap path uses the scoped repository when a workspace is bound.
    - The binary still writes setup failures to stderr, not stdout.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-mcp --lib config</automated>
  </verify>
  <done>The MCP server can enter workspace-bound or shared mode from the same startup path.</done>
</task>

<task type="auto">
  <name>Task 2: Document the workspace-binding contract in project config and MCP README</name>
  <files>opencode.json, crates/mcp/README.md</files>
  <read_first>opencode.json, crates/mcp/README.md, .planning/research/08-RESEARCH.md</read_first>
  <action>Update the project-scoped OpenCode MCP server config to pass `WORKSPACE_ID` through the server environment using env substitution (`{env:DOCRACY_WORKSPACE_ID}`), and document the contract in `crates/mcp/README.md`. Explain that workspace-bound sessions see their workspace plus the shared global governance rows, that the global/shared fallback still works when `WORKSPACE_ID` is omitted, and that scope comes from client config/environment rather than repository-path heuristics.</action>
  <acceptance_criteria>
    - `opencode.json` contains `WORKSPACE_ID` wiring in the MCP environment block.
    - `crates/mcp/README.md` explains the workspace-binding contract and fallback model.
    - The docs still preserve the fixed `./governance` startup contract.
  </acceptance_criteria>
  <verify>
    <automated>grep -n 'WORKSPACE_ID\|DOCRACY_WORKSPACE_ID\|global/shared' opencode.json crates/mcp/README.md</automated>
  </verify>
  <done>The checked-in client example and docs show how to bind a workspace explicitly.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the stdio startup safety under a workspace-bound environment</name>
  <files>crates/mcp/tests/stdio_binary_smoke.rs</files>
  <read_first>crates/mcp/tests/stdio_binary_smoke.rs, crates/mcp/src/bin/docracy-mcp.rs, crates/mcp/src/config.rs</read_first>
  <action>Extend the smoke test to run the binary with both `DATABASE_URL` and a dummy `WORKSPACE_ID` value so the new environment path is exercised while preserving the stdio contract. Keep the assertion focused on transport safety: startup failures still write nothing to stdout and still surface the setup envelope on stderr. Do not couple the test to a live Postgres workspace or any MCP tool call.</action>
  <acceptance_criteria>
    - The smoke test exercises the workspace env path.
    - The smoke test still proves stdout stays empty on startup failure.
    - The smoke test still proves the setup envelope appears on stderr.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-mcp --test stdio_binary_smoke</automated>
  </verify>
  <done>The MCP process remains transport-safe even when workspace binding is present.</done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-mcp --lib config` passes.
- `cargo test -p docracy-mcp --test stdio_binary_smoke` passes.
- The docs and project config show the same workspace-binding contract.
</verification>

<success_criteria>
- Project-scoped MCP clients can bind a workspace explicitly.
- The runtime keeps the bound workspace for the process lifetime.
- Users can still fall back to the shared/global path when needed.
</success_criteria>

<output>
After completion, create `.planning/phases/08-workspace-tenancy-via-mcp-session-binding-and-postgres-rls-isolation/08-02-SUMMARY.md`
</output>
