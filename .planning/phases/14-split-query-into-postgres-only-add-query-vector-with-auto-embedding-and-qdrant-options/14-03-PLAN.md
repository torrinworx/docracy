---
phase: 14-split-query-into-postgres-only-add-query-vector-with-auto-embedding-and-qdrant-options
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - crates/cli/src/main.rs
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "CLI `query` continues to work for guided/raw Postgres queries and no longer accepts vector embeddings."
    - "CLI exposes a new `query-vector` (or `query-vector` subcommand) that supports auto-embedding via Ollama when `embedding` is omitted."
    - "`query-vector` returns the same JSON `QueryResult` shape as `query`."
  artifacts:
    - path: crates/cli/src/main.rs
      provides: "CLI subcommand wiring for query-vector"
      contains: "QueryVector"
  key_links:
    - from: crates/cli/src/main.rs
      to: crates/postgres/src/lib.rs
      via: "docracy_postgres::ollama_embed_text"
      pattern: "ollama_embed_text"
    - from: crates/cli/src/main.rs
      to: crates/core/src/service.rs
      via: "docracy_core::query_vector_documents"
      pattern: "query_vector_documents"
---

<objective>
Add a dedicated CLI entrypoint for vector search (`query-vector`) that can auto-embed query text via Ollama and then execute the new core `query_vector_documents` use-case, while keeping the existing `query` command Postgres-only.

Purpose: make the split visible and usable from the CLI without requiring callers to pre-compute embeddings.
Output: new CLI subcommand + input parsing + output wiring.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@crates/cli/src/main.rs
@crates/core/src/query.rs
@crates/core/src/service.rs
@crates/postgres/src/lib.rs

<interfaces>
Core vector query entrypoint (added in 14-01):
```rust
pub async fn query_vector_documents(repo: &dyn Repository, input: QueryVectorInput) -> Result<QueryResult, CoreError>;
```

Postgres embed helper (added in 14-02):
```rust
pub async fn ollama_embed_text(input: &str, embed_model: Option<&str>) -> Result<Vec<f32>, RepoError>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CLI QueryVector subcommand + auto-embedding input contract</name>
  <files>crates/cli/src/main.rs</files>
  <read_first>
    - crates/cli/src/main.rs
    - crates/core/src/query.rs
    - crates/core/src/service.rs
    - crates/postgres/src/lib.rs
  </read_first>
  <action>
    In `crates/cli/src/main.rs`:
    1) Add a new `Command::QueryVector { input: Option<String> }` subcommand.
    2) Define a local `#[derive(Deserialize)]` input struct for the CLI JSON contract:
       - `query: Option<String>` (text to embed)
       - `embedding: Option<Vec<f32>>` (precomputed embedding)
       - `embed_model: Option<String>` (optional override)
       - `where_` (serde rename "where", default Map)
       - `select: Vec<String>` (default empty)
       - `limit: Option<u32>`
    3) In the command handler:
       - Parse input JSON using the existing `read_json_input` helper.
       - Compute `embedding`:
         - If `embedding` is Some(vec) -> use it.
         - Else require `query` Some(non-empty) -> call `docracy_postgres::ollama_embed_text(query, embed_model.as_deref()).await?`.
         - Else return a structured validation error (use the existing CLI error envelope) with message: `"query_vector requires either embedding or query"`.
       - Call the new core use-case:
         - `docracy_core::query_vector_documents(&repo, docracy_core::query::QueryVectorInput { embedding, where_, select, limit }).await?`
       - Print the returned `QueryResult` JSON (same printing path as other commands).

    4) Keep `Command::Query` wired to `QueryInput` (now Postgres-only) with no embedding behavior.
  </action>
  <acceptance_criteria>
    - `crates/cli/src/main.rs` contains a new `QueryVector` subcommand.
    - `crates/cli/src/main.rs` calls `docracy_postgres::ollama_embed_text` when `embedding` is missing.
    - `crates/cli/src/main.rs` calls `docracy_core::query_vector_documents`.
    - `cargo test -p docracy-cli --quiet` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-cli --quiet</automated>
  </verify>
  <done>
    CLI supports a dedicated vector query subcommand with auto-embedding and returns QueryResult JSON.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-cli` passes.
- `cargo build` still succeeds for the workspace (compile-time assurance of cross-crate API changes).
</verification>

<success_criteria>
- Users can keep using `docracy query` for Postgres-only queries.
- Users can use `docracy query-vector` to run semantic search without precomputing embeddings.
</success_criteria>

<output>
After completion, create `.planning/phases/14-split-query-into-postgres-only-add-query-vector-with-auto-embedding-and-qdrant-options/14-03-SUMMARY.md`
</output>
