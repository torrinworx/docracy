---
phase: 13-async-ollama-embedding-worker-and-qdrant-only-vector-index
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/vector.rs
  - crates/core/src/lib.rs
  - migrations/0008_embedding_job_queue.sql
  - crates/postgres/src/lib.rs
  - crates/postgres/tests/embedding_queue_integration.rs
autonomous: true
requirements:
  - IDX-01
  - IDX-03
must_haves:
  truths:
    - "Document writes enqueue a retryable embedding job snapshot instead of storing vector blobs in Postgres."
    - "A newer revision or embedding model overwrites the prior pending job in place for the same workspace/document/model tuple."
    - "Queue state captures enough metadata for the worker to retry failed jobs without touching canonical document history."
  artifacts:
    - path: crates/core/src/vector.rs
      provides: "Embedding job snapshot contract and canonical text helper"
      min_lines: 40
    - path: migrations/0008_embedding_job_queue.sql
      provides: "Workspace-scoped pending job queue"
      contains: "embedding_job_queue"
    - path: crates/postgres/src/lib.rs
      provides: "Same-transaction enqueue from document writes"
      contains: "ON CONFLICT"
    - path: crates/postgres/tests/embedding_queue_integration.rs
      provides: "Overwrite and state-capture regression coverage"
      min_lines: 40
  key_links:
    - from: crates/postgres/src/lib.rs
      to: migrations/0008_embedding_job_queue.sql
      via: "same-transaction queue insert/update"
      pattern: "embedding_job_queue"
    - from: crates/core/src/vector.rs
      to: crates/postgres/src/lib.rs
      via: "canonical JSON stringification of document content"
      pattern: "canonical_embedding_source_text"
    - from: crates/postgres/tests/embedding_queue_integration.rs
      to: crates/postgres/src/lib.rs
      via: "queue overwrite / archive-state regression"
      pattern: "embedding_job_queue"
---

<objective>
Move vector derivation out of the document write path by defining a durable embedding-job contract and queue so the async Ollama worker can index current document state without changing canonical CRUD behavior.

Purpose: establish a retryable job snapshot boundary before building the worker runtime.
Output: core job contract, queue migration, enqueue path, and regression coverage.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
@crates/core/src/vector.rs
@crates/core/src/lib.rs
@crates/postgres/src/lib.rs
@migrations/0007_vector_mirror_queue.sql
@crates/postgres/tests/postgres_integration.rs

<interfaces>
From `crates/core/src/vector.rs`:
```rust
pub struct VectorMirrorRecord {
    pub workspace_id: Uuid,
    pub document_id: DocumentId,
    pub revision_id: RevisionId,
    pub archived_at: Option<DateTime<Utc>>,
    pub deleted_at: Option<DateTime<Utc>>,
    pub embedding: Vec<f32>,
}

pub struct VectorQueryInput {
    pub query: DocumentQuery,
    pub embedding: Vec<f32>,
}
```

From `crates/postgres/src/lib.rs`:
```rust
pub struct PgRepository {
    pool: PgPool,
    workspace_id: Option<WorkspaceUuid>,
}

pub async fn create_workspace(&self, id: WorkspaceUuid) -> Result<(), RepoError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the embedding job snapshot contract</name>
  <files>crates/core/src/vector.rs, crates/core/src/lib.rs</files>
  <behavior>
    - Test 1: the core crate exports a new `EmbeddingJobRecord` type.
    - Test 2: the job snapshot carries workspace, document, revision, model, source text, and archive/delete metadata.
    - Test 3: canonical text helper returns stable JSON text for the same document content.
  </behavior>
  <read_first>
    - crates/core/src/vector.rs
    - crates/core/src/lib.rs
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Add a public `EmbeddingJobRecord` and a small `canonical_embedding_source_text(&serde_json::Value) -> String` helper in `crates/core/src/vector.rs`, then re-export them from `crates/core/src/lib.rs`. Keep `VectorMirrorRecord` and `VectorQueryInput` unchanged; this phase adds a separate job snapshot boundary for the async worker and must not alter the existing vector-query contract.
  </action>
  <acceptance_criteria>
    - `crates/core/src/vector.rs` contains `EmbeddingJobRecord` and `canonical_embedding_source_text`.
    - `crates/core/src/lib.rs` re-exports the new embedding job symbols.
    - `cargo test -p docracy-core vector -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-core vector -- --nocapture</automated>
  </verify>
  <done>
    Core exposes the immutable snapshot data the Postgres adapter and worker will share for phase 13.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the embedding job queue and enqueue path</name>
  <files>migrations/0008_embedding_job_queue.sql, crates/postgres/src/lib.rs</files>
  <behavior>
    - Test 1: create/update writes insert one pending embedding job per workspace/document/model.
    - Test 2: a newer revision overwrites the older pending row instead of leaving stale work behind.
    - Test 3: the queue row stores canonical source text plus retry metadata for later worker processing.
  </behavior>
  <read_first>
    - migrations/0007_vector_mirror_queue.sql
    - crates/postgres/src/lib.rs
    - crates/core/src/vector.rs
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Create `migrations/0008_embedding_job_queue.sql` with an `embedding_job_queue` table keyed by `(workspace_id, document_id, embed_model)`, storing `revision_id`, `source_text`, `archived_at`, `deleted_at`, `attempt_count`, `last_error`, `available_at`, `claimed_at`, `created_at`, and `modified_at`, plus the same workspace RLS pattern used elsewhere. In `crates/postgres/src/lib.rs`, derive `source_text` with `canonical_embedding_source_text(&doc.content)` during create/update and write the pending row with `ON CONFLICT` overwrite semantics inside the same transaction that persists the document and revision.
  </action>
  <acceptance_criteria>
    - `migrations/0008_embedding_job_queue.sql` contains `embedding_job_queue` and `UNIQUE (workspace_id, document_id, embed_model)` (or an equivalent primary key).
    - `crates/postgres/src/lib.rs` contains a queue insert/update that uses `canonical_embedding_source_text`.
    - `rg -n "embedding_job_queue|canonical_embedding_source_text|ON CONFLICT" migrations/0008_embedding_job_queue.sql crates/postgres/src/lib.rs` finds all three patterns.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres embedding_queue -- --nocapture</automated>
  </verify>
  <done>
    Document writes now leave a retryable embedding job snapshot behind for the worker instead of embedding synchronously.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove queue overwrite and retryable snapshot behavior</name>
  <files>crates/postgres/tests/embedding_queue_integration.rs</files>
  <read_first>
    - crates/postgres/tests/postgres_integration.rs
    - crates/postgres/src/lib.rs
    - migrations/0008_embedding_job_queue.sql
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Add focused Postgres integration coverage for the new queue contract: a later revision must overwrite the existing row in place, archive/delete timestamps must be reflected in the queued snapshot, and the queue must remain one row per workspace/document/model. Keep the tests narrowly scoped so they prove the queue contract without pulling the worker runtime into this plan.
  </action>
  <acceptance_criteria>
    - `crates/postgres/tests/embedding_queue_integration.rs` exists and contains overwrite/state regression tests.
    - `cargo test -p docracy-postgres embedding_queue_integration -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres embedding_queue_integration -- --nocapture</automated>
  </verify>
  <done>
    The queue contract is locked by tests and is ready for the Ollama worker to consume.
  </done>
</task>

</tasks>

<verification>
- Confirm the core job snapshot type exists before the Postgres adapter references it.
- Confirm the new queue table uses workspace scoping and overwrite semantics, not append-only history.
- Confirm the integration tests cover the queue boundary without invoking Ollama or Qdrant yet.
</verification>

<success_criteria>
- Document writes enqueue a retryable embedding job snapshot in the same Postgres transaction.
- The queue key prevents stale rows from surviving newer revisions or model changes.
- Phase 13 plan 02 can consume the queue contract without redefining the snapshot shape.
</success_criteria>

<output>
After completion, create `.planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-01-SUMMARY.md`
</output>
