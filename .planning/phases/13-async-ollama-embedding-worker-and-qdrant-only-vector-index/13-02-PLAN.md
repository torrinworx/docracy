---
phase: 13-async-ollama-embedding-worker-and-qdrant-only-vector-index
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - crates/postgres/Cargo.toml
  - crates/postgres/src/indexer.rs
  - crates/postgres/src/bin/docracy-indexer.rs
  - crates/postgres/src/vector.rs
  - crates/postgres/tests/indexer_integration.rs
  - .env.example
  - README.md
autonomous: true
requirements:
  - IDX-02
  - IDX-03
must_haves:
  truths:
    - "A workspace-scoped worker can claim pending embedding jobs, call Ollama, and generate vectors from canonical document text."
    - "Successful embedding jobs upsert into Qdrant and clear the queue row only after the vector write succeeds."
    - "Ollama or Qdrant failures leave the job retryable with observable attempts/error metadata."
    - "Local docs show how to run the worker with the repo workspace env and local Ollama/Qdrant services."
  artifacts:
    - path: crates/postgres/src/indexer.rs
      provides: "Async worker loop and Ollama client"
      min_lines: 80
    - path: crates/postgres/src/bin/docracy-indexer.rs
      provides: "Worker entrypoint"
      contains: "main"
    - path: crates/postgres/src/vector.rs
      provides: "Qdrant collection and upsert helpers"
      contains: "docracy_workspace_"
    - path: crates/postgres/tests/indexer_integration.rs
      provides: "Mock Ollama/Qdrant worker regression coverage"
      min_lines: 60
    - path: README.md
      provides: "Worker startup and local runbook"
      contains: "docracy-indexer"
    - path: .env.example
      provides: "Worker env examples"
      contains: "OLLAMA_URL"
  key_links:
    - from: crates/postgres/src/indexer.rs
      to: "http://127.0.0.1:11434/api/embed"
      via: "Ollama embed POST"
      pattern: "/api/embed"
    - from: crates/postgres/src/indexer.rs
      to: crates/postgres/src/vector.rs
      via: "workspace-scoped Qdrant upsert"
      pattern: "docracy_workspace_"
    - from: README.md
      to: .env.example
      via: "documented local env contract"
      pattern: "OLLAMA_URL|OLLAMA_EMBED_MODEL|QDRANT_URL|WORKSPACE_ID"
---

<objective>
Build the async worker that claims embedding jobs, calls Ollama, and writes vectors into Qdrant so the derived index stays current without blocking document writes.

Purpose: turn the retryable queue contract from plan 01 into a usable local indexing process.
Output: worker binary, Ollama/Qdrant pipeline, integration tests, and operator docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
@.planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-01-PLAN.md
@crates/postgres/Cargo.toml
@crates/postgres/src/vector.rs
@crates/postgres/src/lib.rs
@crates/postgres/tests/postgres_integration.rs

<interfaces>
From `crates/postgres/src/vector.rs`:
```rust
pub fn qdrant_collection_name(workspace_id: WorkspaceUuid) -> String;

pub(crate) async fn qdrant_search_point_ids(
    collection: &str,
    embedding: &[f32],
    limit: usize,
) -> Result<Vec<String>, RepoError>;
```

From Ollama `/api/embed`:
```json
{
  "model": "embeddinggemma",
  "input": "Why is the sky blue?"
}
```

Response shape:
```json
{
  "model": "embeddinggemma",
  "embeddings": [[0.01, -0.001, 0.05]]
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the worker binary and queue-drain runtime</name>
  <files>crates/postgres/Cargo.toml, crates/postgres/src/indexer.rs, crates/postgres/src/bin/docracy-indexer.rs</files>
  <behavior>
    - Test 1: the binary reads `WORKSPACE_ID`, `OLLAMA_URL`, `OLLAMA_EMBED_MODEL`, `QDRANT_URL`, `POLL_INTERVAL_MS`, and `BATCH_SIZE`.
    - Test 2: the worker claims pending rows with `FOR UPDATE SKIP LOCKED` and never writes MCP/CLI JSON to stdout.
    - Test 3: the worker binds the Postgres session to the requested workspace before polling.
  </behavior>
  <read_first>
    - crates/postgres/Cargo.toml
    - crates/postgres/src/lib.rs
    - crates/postgres/src/vector.rs
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Add a `docracy-indexer` binary target in `crates/postgres/Cargo.toml` and create `crates/postgres/src/indexer.rs` as the async runtime for the worker. Parse `WORKSPACE_ID` plus `OLLAMA_URL=http://127.0.0.1:11434`, `OLLAMA_EMBED_MODEL=embeddinggemma`, `QDRANT_URL=http://127.0.0.1:6333`, `POLL_INTERVAL_MS`, and `BATCH_SIZE`; connect through `PgRepository::connect_scoped` so the worker only sees the bound workspace’s queue rows; and implement a poll/claim loop that uses `FOR UPDATE SKIP LOCKED` to lease pending jobs before embedding them.
  </action>
  <acceptance_criteria>
    - `crates/postgres/Cargo.toml` contains a `[[bin]]` entry for `docracy-indexer`.
    - `crates/postgres/src/indexer.rs` contains the worker config env names and `FOR UPDATE SKIP LOCKED`.
    - `cargo test -p docracy-postgres indexer -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres indexer -- --nocapture</automated>
  </verify>
  <done>
    The repository can start a workspace-scoped embedding worker process without changing the document tool surface.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Embed through Ollama and upsert Qdrant points</name>
  <files>crates/postgres/src/vector.rs, crates/postgres/tests/indexer_integration.rs</files>
  <behavior>
    - Test 1: the worker sends `POST /api/embed` with the configured model and canonical source text.
    - Test 2: successful embeddings create or reuse the workspace-scoped Qdrant collection and upsert the document id point.
    - Test 3: failed Ollama or Qdrant requests leave the queue row retryable with incremented attempts and a last-error string.
  </behavior>
  <read_first>
    - crates/postgres/src/vector.rs
    - crates/postgres/src/indexer.rs
    - crates/postgres/tests/postgres_integration.rs
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Reuse the existing Qdrant REST helper pattern in `crates/postgres/src/vector.rs` to upsert vectors into `docracy_workspace_{workspace_id}` collections, keeping `document_id` as the point id and including `workspace_id`, `revision_id`, `embed_model`, `archived_at`, `deleted_at`, and `embedding_dimension` in the payload. In `crates/postgres/src/indexer.rs`, call Ollama’s `/api/embed` endpoint, turn the first returned embedding into the Qdrant vector, mark the queue row complete only after the Qdrant upsert succeeds, and on failure update the row with `attempt_count += 1`, `last_error`, and a future `available_at` so the next poll can retry.
  </action>
  <acceptance_criteria>
    - `crates/postgres/src/vector.rs` contains `docracy_workspace_` collection naming and point upsert payload fields including `embed_model`.
    - `crates/postgres/tests/indexer_integration.rs` contains success and retry regression tests against mock Ollama/Qdrant services.
    - `cargo test -p docracy-postgres indexer_integration -- --nocapture` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres indexer_integration -- --nocapture</automated>
  </verify>
  <done>
    The async worker can turn a pending job into a durable Qdrant point and leave failures safely retryable.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document the local worker runbook and env contract</name>
  <files>.env.example, README.md</files>
  <read_first>
    - .env.example
    - README.md
    - compose.yml
    - .planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-RESEARCH.md
  </read_first>
  <action>
    Add the exact worker configuration and startup instructions to the repo docs. Include `WORKSPACE_ID`, `OLLAMA_URL`, `OLLAMA_EMBED_MODEL`, `QDRANT_URL`, and the `cargo run -p docracy-postgres --bin docracy-indexer` command. Explain that the worker is workspace-scoped, Postgres remains canonical, Qdrant is the derived index, and local Ollama must have the embedding model pulled before the worker can index documents.
  </action>
  <acceptance_criteria>
    - `.env.example` contains the worker env vars and default local URLs.
    - `README.md` contains `docracy-indexer` and the workspace/env contract.
    - `rg -n "docracy-indexer|WORKSPACE_ID|OLLAMA_URL|OLLAMA_EMBED_MODEL|QDRANT_URL" README.md .env.example` finds all required strings.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "docracy-indexer|WORKSPACE_ID|OLLAMA_URL|OLLAMA_EMBED_MODEL|QDRANT_URL" README.md .env.example</automated>
  </verify>
  <done>
    A developer can start the worker locally with the same env model used by the repository’s workspace-scoped runtime.
  </done>
</task>

</tasks>

<verification>
- Confirm the worker reads workspace scope from env and does not require new client-facing tool changes.
- Confirm Ollama requests are explicit and point to the embed endpoint, not chat/completion APIs.
- Confirm Qdrant payloads keep revision/model/archive metadata so the derived index can be rebuilt.
</verification>

<success_criteria>
- Pending embedding jobs can be drained by a workspace-scoped worker.
- Qdrant receives workspace-scoped vector upserts generated by Ollama.
- Phase 13 has a documented local runbook and retry semantics that keep the index rebuildable.
</success_criteria>

<output>
After completion, create `.planning/phases/13-async-ollama-embedding-worker-and-qdrant-only-vector-index/13-02-SUMMARY.md`
</output>
