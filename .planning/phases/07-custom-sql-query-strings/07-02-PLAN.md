---
phase: 07-custom-sql-query-strings
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/postgres/src/lib.rs
  - crates/postgres/tests/postgres_integration.rs
  - README.md
  - crates/mcp/README.md
autonomous: true
requirements:
  - TST-01
  - DOC-01
must_haves:
  truths:
    - "Raw SQL queries run against Postgres in read-only mode and cannot mutate data."
    - "Caller-supplied timeout and row limits are clamped to server ceilings before execution."
    - "The public README and MCP README explain the `sql` field, precedence rules, and ceilings."
  artifacts:
    - path: "crates/postgres/src/lib.rs"
      provides: "Bounded read-only raw SQL execution and JSON row materialization"
    - path: "crates/postgres/tests/postgres_integration.rs"
      provides: "End-to-end raw SQL adapter coverage"
    - path: "README.md"
      provides: "User-facing query contract documentation"
    - path: "crates/mcp/README.md"
      provides: "MCP query payload documentation"
  key_links:
    - from: "PgRepository::query_raw_documents"
      to: "SET TRANSACTION READ ONLY"
      via: "transaction-level read-only enforcement"
    - from: "PgRepository::query_raw_documents"
      to: "to_jsonb(raw_query)"
      via: "dynamic row materialization without manual column typing"
    - from: "README.md"
      to: "crates/mcp/README.md"
      via: "shared query payload contract"
---

<objective>
Execute raw SQL safely in the Postgres adapter and document the new query contract for CLI and MCP users.

Purpose: Agents need a direct SQL escape hatch that still cannot write data, cannot exceed server-defined ceilings, and is explained clearly enough to use without guesswork.
Output: Bounded raw SQL execution in `docracy-postgres`, integration tests, and updated public docs.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md
@crates/core/src/query.rs
@crates/postgres/src/lib.rs
@crates/postgres/tests/postgres_integration.rs
@README.md
@crates/mcp/README.md

<interfaces>
From `crates/postgres/src/lib.rs`:
```rust
pub struct PgRepository {
    pub fn new(pool: PgPool) -> Self;
    pub fn pool(&self) -> &PgPool;
    pub async fn connect(database_url: &str) -> Result<Self, sqlx::Error>;
    pub async fn migrate(&self) -> Result<(), sqlx::migrate::MigrateError>;
}
```

From `crates/postgres/tests/postgres_integration.rs`:
```rust
async fn isolated_repo(url: &str) -> (PgRepository, SchemaGuard);
async fn assert_index_exists(repo: &PgRepository, index_name: &str);
async fn assert_index_absent(repo: &PgRepository, index_name: &str);
```

From `README.md`:
```md
Current `Query` payload:
{ "query": "...", "where": { ... }, "order_by": [ ... ], "select": [ ... ], "limit": 10 }
```

From `crates/mcp/README.md`:
```md
Input (matches `docracy_core::query::QueryInput`):
{ "query": "...", "where": {}, "order_by": [ ... ], "select": [ ... ], "limit": 50, "cursor": "..." }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement bounded read-only raw SQL execution in the Postgres adapter</name>
  <files>crates/postgres/src/lib.rs</files>
  <read_first>crates/postgres/src/lib.rs, crates/core/src/query.rs, crates/core/src/repository.rs, migrations/0001_documents_and_revisions.sql, migrations/0003_content_search.sql</read_first>
  <action>Add `PgRepository::query_raw_documents(...)` and the supporting helpers needed to execute a single caller-supplied `SELECT` in a read-only transaction. Clamp caller values to server ceilings of `100` rows and `5000ms`, set `statement_timeout` with the clamped timeout, reject multi-statement input, and materialize each row as JSON with `to_jsonb(...)` so the adapter does not need to guess column types. Execute the raw SQL twice when necessary: once for an exact `COUNT(*)` and once for the capped result page. Preserve the existing guided `query_documents` path unchanged.</action>
  <acceptance_criteria>
    - Raw SQL runs inside a read-only transaction.
    - The helper clamps row limits to `100` and timeouts to `5000ms`.
    - The adapter returns JSON rows without manual per-column type mapping.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres --lib</automated>
  </verify>
  <done>The Postgres adapter can execute bounded read-only raw SQL safely.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the raw SQL path with real Postgres integration tests</name>
  <files>crates/postgres/tests/postgres_integration.rs</files>
  <read_first>crates/postgres/tests/postgres_integration.rs, crates/postgres/src/lib.rs, migrations/0001_documents_and_revisions.sql</read_first>
  <action>Add end-to-end tests that seed a few documents, run a raw SQL `SELECT` against the canonical tables, and assert the returned rows are JSON maps. Add a second assertion that a raw SQL `UPDATE` fails because the transaction is read-only, and a third assertion that a caller limit above the ceiling is truncated to the configured maximum. Keep the tests scoped to the existing isolated-schema harness so they run against the real adapter and migrations.</action>
  <acceptance_criteria>
    - The integration test module exercises raw SQL against the real Postgres adapter.
    - A write statement inside raw SQL fails.
    - A request above the row ceiling returns no more than `100` rows.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p docracy-postgres --test postgres_integration raw_sql</automated>
  </verify>
  <done>Raw SQL behavior is locked with real adapter coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Document the sql field and ceiling rules</name>
  <files>README.md, crates/mcp/README.md</files>
  <read_first>README.md, crates/mcp/README.md, crates/core/src/query.rs</read_first>
  <action>Update the public query examples to show a raw SQL request with a single `sql` field, `limit`, and `timeout_ms`. Explain that `sql` takes precedence over the guided `query/where/order_by/select` fields, that the server clamps the request to `100` rows and `5000ms`, and that raw SQL is read-only. Keep the guided fallback example visible so agents still know how the legacy path works when `sql` is omitted.</action>
  <acceptance_criteria>
    - `README.md` contains a raw SQL example with `"sql"`, `limit`, and `timeout_ms`.
    - `crates/mcp/README.md` documents the same `sql` field and precedence rule.
    - Both docs mention the `100`-row and `5000ms` ceilings.
  </acceptance_criteria>
  <verify>
    <automated>grep -n '"sql"\|5000\|100' README.md crates/mcp/README.md</automated>
  </verify>
  <done>The raw SQL query contract is documented for CLI and MCP users.</done>
</task>

</tasks>

<verification>
- `cargo test -p docracy-postgres --lib` passes.
- `cargo test -p docracy-postgres --test postgres_integration raw_sql` passes when a test database is available.
- The README and MCP README describe the raw SQL mode, fallback mode, and ceilings with the same values used by the adapter.
</verification>

<success_criteria>
- Raw SQL is executed by the Postgres adapter in read-only mode.
- Caller-supplied row/time limits are capped server-side.
- The public docs explain how to use the single `sql` field and when the guided path still applies.
</success_criteria>

<output>
After completion, create `.planning/phases/07-custom-sql-query-strings/07-02-SUMMARY.md`
</output>
