[package]
name = "docracy-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
docracy-core = { path = "../core" }
docracy-postgres = { path = "../postgres" }

[lib]
path = "src/lib.rs"
